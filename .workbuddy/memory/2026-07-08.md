# 2026-07-08 工作日志

## 完成事项

### 服务重启
- 服务器/客户端进程因系统休眠或超时停止，重新启动
- 后端 Express：`localhost:3001` 正常运行
- 前端 Vite：`localhost:5174`（5173 被占用，自动切换）

### UI 重构：全局侧边栏导航（✅ 完成）
- SOP 流程：PM(PRD) → 架构师(系统设计+路由方案) → 工程师(17文件) → QA(15/15通过)
- 左侧 7 模块侧边栏：项目概览/项目计划/待办/物料/故障/会议/周报 + 新建项目弹窗
- 路由扁平化：`/projects/:id/xxx` → `/xxx?projectId=yyy`，旧路由自动重定向
- 每页面顶部 ProjectSelector（Autocomplete），支持跨项目全局视图
- 删除 CreateProjectPage/ProjectDetailPage
- 提交 `45e6711` 已推送

### 看板模块全功能开发（✅ 完成）
- SOP 流程：PM(PRD+7待确认) → 架构师(DB设计+5任务分解) → 工程师(24文件) → QA(20/20通过)
- **后端**：新表 subtasks、tasks新增 sort_order/completed_at、projects新增 theme_color
- **优先级四级**：urgent#cf1322 / high#ff4d4f / medium#faad14 / low#52c41a（含P0-P2迁移）
- **6 新 API**：子任务 CRUD、reorder、toggle-complete、kanban-stats
- **前端 11 组件 + 2 hooks**：双栏项目看板、全局四列看板、@dnd-kit 拖拽、子任务面板、乐观更新、滚动保持、自动折叠
- QA 发现 2 个 DB 语法 Bug（全部修复），20/20 通过
- 提交 `83453e9` 已推送

### 看板页面重构：多项目网格（✅ 完成）
- 快速模式：TaskKanbanPage 重写 → 多项目看板网格布局
- 去掉 ProjectSelector，改为加载全部项目 + 全部任务，按 project_id 分组
- 响应式网格 `repeat(auto-fill, minmax(540px, 1fr))`，每项目一个 KanbanProjectView 卡片
- "新建项目看板"按钮 → CreateProjectDialog
- 所有 kanban 子组件零改动复用
- 提交 `d4b4ae6` 已推送
