# 2026-07-15 工作日志

## 1. 网站挂了 → 运维 SOP 恢复（上午）
- 现象：网页打不开，PM2 进程列表为空（情况A：daemon 未启动）
- 处理：`npx pm2 start ecosystem.config.js` → forge online → 健康检查 App/API 均 200 → `pm2 save`
- 隐患：内存占用 89%（SOP 提示 81%+ 不稳），建议确认开机自启注册表 HKCU\Run 的 HPM-PM2-AutoStart

## 2. 会议纪要模块支持「全时会议」（核心交付）
- 背景：用户通过全时 App 内「获取纪要」拿到分享链接（aiminutes.quanshimeet.cn/summary/m/<ID>?yhy_p=e），无开放 API；WebFetch 验证该页为登录态动态加载，正文无法自动抓取
- 方案：「存链接 + 跳转」，与用户现有手动流程一致（非 API 实时同步）
- 代码改动：
  - `server/src/db.js`：meetings 表迁移新增 `minutes_url TEXT`
  - `server/src/routes/meetings.js`：POST/PUT 接受 minutes_url/external_id/meeting_code；GET /:id/minutes 全时返回 `{source:"link",url}`
  - `client/src/components/meeting/CreateMeetingDialog.jsx`（新增）：平台选择(全时/腾讯/手动) + 全时链接自动解析会议 ID
  - `client/src/pages/MeetingListPage.jsx`：新建会议按钮 + 平台筛选(全部/腾讯/全时/手动) + 平台徽标列
  - `client/src/components/meeting/MeetingDrawer.jsx`：平台徽标 + 「打开全时纪要」按钮
  - `client/src/components/meeting/SmartMinutesViewer.jsx`：link 类型渲染按钮
- 验证：用真实全时链接实测 POST 创建(id=43) → minutes 接口返回 link 数据 → 清理测试数据；build 通过；forge 重启 200
- CHANGELOG 已更新

## 待办/备注
- 全时会议 v2 PRD 原列为「明确不做」，本次用户明确要求，已落地实现（决策变更）

## 3. 项目计划「节点任务」逻辑重构（下午）
- 用户三项诉求：①解除“先普通后转节点才能定日期”限制，类型切换允许改日期但默认工期恒1天；②甘特图显示红色菱形里程碑着重显示；③节点任务可被其他任务依赖
- 调研发现：节点在 `schedule_tasks` 表 `task_type` 字段（普通/阶段/节点），原逻辑强制 `is_locked=1` + 后端 400 拦截，导致日期锁死且不可被前置依赖联动看出
- 改动（已提交 9990ac5，已 push）：
  - `server/src/routes/schedule.js`：PUT 移除节点 400 保护；转节点时折叠为单日里程碑(结束=开始)、工期强制1天、不再强制 is_locked；新增节点日期可改分支（忽略 duration_days 修改）；generate 解耦节点与 is_locked
  - `client/src/components/schedule/ScheduleTable.jsx`：编辑守卫允许节点改起止、禁改工期
  - `client/src/components/schedule/GanttRow.jsx`：节点渲染红色菱形(#EF4444+外发光)
  - `client/src/components/schedule/PredecessorDialog.jsx`：节点候选加 ◆ 标记
- 验证：脚本对运行服务实测（throwaway 任务建删），确认节点转类型→dur=1/end=start、日期可改返回200、duration 修改被忽略、作为前置时下游 start=节点 end+1；build 0 错；forge 重启 200
- 结论：依赖对话框本就未过滤节点(只排除自身)，“可被依赖”原本成立，本次主要在甘特图侧将联动效果显性化
- CHANGELOG 已更新
