# 2026-07-10 工作日志

## BugFix: PM2 开机自启固化（彻底解决重启后服务丢失）

### 问题根因
- Windows 上 `pm2 startup` 报错 "Init system not found"（PM2 原生不支持 Windows 开机自启）
- 重启后 PM2 daemon 不启动，进程列表丢失，服务全部挂掉
- 之前的 PM2 配置只有崩溃自动重启（autorestart），没有开机自启

### 修复方案
- **注册表 HKCU\Run** 注册 `HPM-PM2-AutoStart` → 指向 `D:\HPM\scripts\pm2-resurrect.bat`
- **bat 脚本**：延迟 15 秒（等系统就绪）→ 用完整 node 路径执行 `pm2 resurrect`
- **pm2 save**：进程列表保存到 `C:\Users\chenxu\.pm2\dump.pm2`
- **备用 VBS**：`scripts/pm2-autostart.vbs`（隐藏窗口版，当前未启用）

### 完整链路
```
开机登录 → 注册表 HKCU\Run 触发 → pm2-resurrect.bat
→ 延迟15秒 → node pm2 resurrect → 从 dump.pm2 恢复 hpm-server + hpm-client
```

### 崩溃恢复（已有）
- PM2 `autorestart: true` — 进程崩溃后自动重启
- `max_memory_restart: 512M` — 内存超限自动重启

### 文件
- `scripts/pm2-resurrect.bat` — 主启动脚本（注册表使用）
- `scripts/pm2-autostart.vbs` — 备用方案（VBS隐藏窗口版）
- `start.sh` — 更新说明，标注开机自启已配置
- 注册表：`HKCU\Software\Microsoft\Windows\CurrentVersion\Run\HPM-PM2-AutoStart`

### 验证
- PM2 状态：hpm-server + hpm-client 均 online
- HTTP 检查：Server(3001)=200, Client(5173)=200
- dump.pm2 文件存在（1.5MB），pm2 save 成功

### 安全策略适配
- `wscript.exe` 被 LOLBin 安全策略拦截 → 改用 bat 文件方案
- bat 路径全 ASCII，内容全 ASCII，无安全问题

### 提交
- b8192d8 已推送到 origin/main

## 运维知识库更新：服务器挂了排查 SOP

将本次排查经验固化为 SOP 写入 `D:\HPM\.workbuddy\memory\MEMORY.md` 的「运维知识库」章节，包含：

- **3种情况分类**：A.进程丢失 / B.进程崩溃 / C.进程在线但异常
- **排查步骤**：pm2 status → HTTP 检查 → 日志分析
- **历史 Bug 参考**：react-dom损坏、Vite --force循环、fastRefresh白屏、NODE_ENV错误
- **PM2 常用命令速查表**：10条核心命令
- **关键文件路径清单**：配置/脚本/日志/dump/node路径
- **开机自启修复方法**：注册表检查+重新注册命令

后续再遇到"服务器挂了"，直接参考 MEMORY.md 的运维知识库章节按 SOP 排查。

## 前端全局视觉升级

### 设计规范（借鉴 design-taste-frontend skill 原则）
- **主色**: #1565C0 → #1E40AF（blue-800，更深沉专业）
- **背景**: #f5f5f5 → #F8FAFC（slate-50 暖白）
- **中性色**: grey 全系替换为 slate 暖灰
- **字体**: 加入 Inter（Google Fonts）+ PingFang SC 回退
- **圆角**: sm6/md8/lg12/xl16 统一规范
- **阴影**: 柔和 rgba(15,23,42,...) 替代 MUI 默认硬阴影

### 布局改版
- **AppBar**: 蓝色实底 → 白底+底部阴影+蓝色 Logo 方块（32x32 圆角 + 白色"H"字）
- **Sidebar**: 220px 平铺 → 240px 3分组（项目管理/协作沟通/数据管理）+ 选中3px蓝色竖条 + hover 态
- **主内容区**: maxWidth 1400 → 1280

### 文件变更（22 个文件，675 行新增）
- 新建 `client/src/theme.js` — 完整主题配置（palette/typography/shape/shadows/components 覆盖）
- 修改 `main.jsx` — 引用 theme.js
- 修改 `index.html` — 加 Inter Google Fonts
- 修改 `Layout.jsx` — AppBar 白底+Logo
- 修改 `Sidebar.jsx` — 3分组+选中竖条
- 17 个文件硬编码色值替换（#1565C0/#f5f5f5/#52c41a/#e0e0e0/#D32F2F/#BDBDBD 全部清零）

### QA 验证
- 7 项检查 6/7 通过，1 项失败（ScheduleTable.jsx 旧色值残留）
- 工程师修复 3 处（含额外发现 #BDBDBD），全局 grep 确认零残留
- Build 通过（vite build 2.13s）

### 提交
- 3086f62 已推送到 origin/main

## Skill 安装：impeccable（前端设计工具集）

- **来源**: WorkBuddy 推荐市场（BuiltinMarket）
- **版本**: v2.0.0
- **安装路径**: `~/.workbuddy/skills/impeccable/`
- **内容**: 31 个 Markdown 设计参考文档（SKILL.md + references/*.md），涵盖视觉风格、布局排版、动效交互、质量保障、设计系统等
- **安全审计**: P2 安全（纯文档型，无可执行代码）
- **用途**: 后续 UI 设计优化时可加载此 skill 获取专业设计指导，比 design-taste-frontend 更全面（后者偏 landing page，impeccable 涵盖 dashboard/data-table 等产品 UI）

## 深色科技紫+毛玻璃+动效高级感升级

### 设计方向（借鉴 impeccable skill 原则）
- **配色**: 浅色蓝白 → 深色科技紫（#0B0815 极深紫黑底 + #8B5CF6 violet-500 主色）
- **毛玻璃**: 策略性使用 backdrop-filter:blur — AppBar(20px)/Drawer(20px)/Card(10px)/Paper(12px)/Dialog(24px)/Menu(16px)/Tooltip(8px)
- **动效**: ease-out-quart/expo 缓动 + 页面淡入上移 + 卡片hover紫色辉光 + 按钮渐变+scale + 输入框focus glow
- **无障碍**: prefers-reduced-motion 媒体查询适配

### 文件变更（20 个文件，455 行新增，194 行删除）
- `client/src/theme.js` — 完全重写为深色模式（mode:dark + grey全系紫调深灰 + 毛玻璃组件覆盖 + 紫色渐变按钮 + 深色滚动条）
- `client/src/animations.css` — 新建动效系统（CSS变量 + keyframes + reduced-motion）
- `client/src/main.jsx` — 引入 animations.css
- `client/src/components/layout/Layout.jsx` — Logo紫色渐变 + page-enter动效
- `client/src/components/layout/Sidebar.jsx` — 紫色选中态 + contained按钮
- 15 个组件/页面色值替换（看板/排期/图表/会议/对话框）

### QA 验证
- 7 大类 30+ 检查点全部通过
- 旧色值零残留（#1E40AF/#F8FAFC/#0F172A/#64748B/#E2E8F0/#F1F5F9）
- Build 通过（vite build 2.42s）

### 提交
- ab597b1 已推送到 origin/main

## 浅色精致紫 + PageHeader 统一化（回退深色 + 统一布局）

### 背景
- 用户反馈深色太深，要求改回浅色
- 核心诉求：所有功能页面风格统一，不是单纯改主色调

### 改动
1. **theme.js 回退浅色**：mode:dark→light，#0B0815→#F9FAFB，移除所有 backdropFilter 毛玻璃
2. **新建 3 个公共组件**：PageHeader（标题+副标题+操作区）、PageLoading（统一加载态）、EmptyState（统一空状态）
3. **9 个页面统一化**：全部用 PageHeader 替代各自标题区，全部用 PageLoading 替代各自的 CircularProgress
4. **深色色值清理**：#0B0815/#15101F/#F5F3FF/#A89BC4/rgba(255,255,255,*)/backdropFilter 全部零残留

### 文件变更（24 个文件，367 行新增，379 行删除）
- 新建：PageHeader.jsx / PageLoading.jsx / EmptyState.jsx
- 重写：theme.js
- 修改：9 个页面 + Layout + Sidebar + 10 个组件色值替换

### QA 验证
- 5 项检查全部通过，NoOne
- 深色色值零残留，build 通过（1.66s）

### 提交
- 793a57d 已推送到 origin/main

## 白屏修复：Vite 缓存损坏

- 症状：PM2 进程在线但页面白屏
- 根因：`node_modules/.vite/` 缓存损坏，浏览器加载 JS 模块抛错 → React 渲染崩溃
- 修复：`rm -rf client/node_modules/.vite` + `pm2 restart hpm-client`
- 提交：无（运维操作，未改代码）

## 6项UI细节优化（03f1551）

### 改动清单
1. **项目概览 - 当前阶段联动**：`ProjectCard.jsx` 从 tasks 数组找 `status === "进行中"` 的任务，显示其 title
2. **项目概览 - 排版**：代号 1.25rem/fontWeight 800 单独行，名称 0.95rem 单独行，InfoRow mb:0.25 加大间距
3. **待办事项 - 对齐修复**：`DoneColumn.jsx` PriorityChip 外层加 display:flex/alignItems:center/alignSelf:center，完成时间加 lineHeight:1
4. **本周会议 - 斜线表头+填充色**：SVG 对角线 + "星期"/"时间" 文字，HEADER_BG="#F3F4F6" 首行首列底色
5. **本周会议 - 卡片区去边框**：coveredByMeeting 判断 → borderBottom:"none"，会议名字号 0.72rem，时间字号 0.65rem
6. **本周会议 - 输出物快速编辑**：InlineOutput 组件 → 点击编辑/Enter或blur保存/Escape放弃

### Bug修复（同提交内）
- `HEADER_BG` 常量未声明 → 修复
- `handleOutputChange` 函数缺失 → 添加 useCallback

### 文件
- `ProjectCard.jsx` — 54行改
- `DoneColumn.jsx` — 14行改
- `WeekMeetingPage.jsx` — 184行改

### 验证
- Vite build 通过（1.92s）
- PM2 重启生效，HTTP 200
- 已推送 origin/main

## DoneColumn 对齐再修复（fe6a809）

### 问题
- 用户反馈 03f1551 的 DoneColumn 对齐修复后仍有问题：状态灯、任务名、完成时间仍不在同一水平线

### 根因
- 外层容器 `alignItems: "center"` 让 PriorityChip 对齐到内容区域垂直中心
- 当任务有子任务时（两行内容），中心点在第一行和第二行之间，导致状态灯与标题错位

### 修复（1文件 3处）
- 第66行：`alignItems: "center"` → `alignItems: "flex-start"`
- 第83行：移除 `alignSelf: "center"`，改为 `mt: "2px"`（10px 圆点与 12px 文字视觉居中对齐）
- 第84行：新增 `pt: 0` 防止默认 padding 干扰

### 验证
- QA 通过（3项修改 + 语法 + 逻辑验证全部 PASS）
- PM2 重启生效
- 已推送 origin/main
