# 2026-08-18 工作日志

## Git 推送修复（阻塞解除）
- 现象：连续多日 `git push` 失败，报错 `could not read Username for 'https://github.com': terminal prompts disabled`。原因：公开仓库 fetch 走匿名不报错，push 才触发认证，而 GCM 取不到凭据。
- 根因：`~/.gitconfig` 中有一行 `credential.helper=`（空值），把凭据助手列表清空，导致 push 回退到交互输入而失败（非交互 shell 无法弹浏览器重登）。
- 修复：将 `~/.gitconfig` 的 `credential.helper=` 改为 `credential.helper=manager`。GCM 实际可从 Windows 凭据管理器取到有效 PAT（`git credential-manager get` 验证通过），只是之前未被调用。
- 结果：成功推送本地领先 origin/main 的 15 个提交（`a48299a..c8b5a9f`），并 `git push -u origin main` 设好上游跟踪；修复后默认 `git push` 已正常（Everything up-to-date）。
- 教训：今后若再遇 git 认证在非交互环境失败，先 `git credential-manager get` 验证凭据是否在 wincred，再查 `.gitconfig` 的 `credential.helper` 是否被空值清空。

## 快速排期：恢复「矩形进度条」功能
- 需求：轨道内增加矩形进度条（style='bar'），体现阶段类任务；底边贴箭头线、纯色填充+白边阴影、文字标注；两端拖拽改起止、整条平移；节点永远最上层。
- 改动（`client/src/pages/QuickSchedulePage.jsx`）：
  - `RectBar` 重构：三模式拖拽（start/end/move 复用 ArrowBar 逻辑），`BAR_TOP=13 + BAR_HEIGHT=18` 使底边 31 正好压箭头线顶边；`border:2px solid #fff + boxShadow` 形成白边阴影；两端圆形 ew-resize 把手（top17 与条中心对齐）。
  - `DraggableMilestone` zIndex 3→4，节点盖过矩形条与箭头线。
  - TrackRow 底部按钮区加「＋进度条」，`EditBarDialog` 支持新增模式（`defaultStart/defaultEnd` + isEdit 判断，新增时隐藏样式下拉、固定 style='bar'）。
  - 主组件新增 `creatingBar` state + `handleAddBar`/`handleCreateBar`（`bars.create` 传 style:'bar'）。

## 构建失败排障（误放 png + start.sh 缺陷）
- 现象：`bash start.sh build` 失败——vite 清空 dist 时 trash `client/public/App_icon_design__a_single_dyna_2026-07-11T10-05-58.png` 报 "Some operations were aborted"；且 build 失败后 `cd ..` 未执行，pm2 在 client 目录找不到 ecosystem.config.js，还把 forge 进程误删（服务短暂挂掉）。
- 处理：① 该 png 是误放进 public 的 AI 生成图（未被 src 引用，forge-package 里有同名副本），已移出 public 到 D:\HPM 根；② `rm -rf client/dist` 清空后重新 `npx vite build` 成功；③ 从 D:\HPM 目录 `pm2 start ecosystem.config.js` 恢复 forge（online，首页 200）。
- 修复 `start.sh` build 分支：`cd client && npm run build && cd ..` 改为 `(cd client && npm run build) || { echo 失败; exit 1; }`，避免 build 失败时误删服务。
- 教训：public 目录应只放 PWA/静态资产，勿放 AI 生成临时图；`bash start.sh build` 失败时会连带 pm2 误删服务，已加失败即退出的保护。
