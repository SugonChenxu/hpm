# 2026-08-17 工作日志

## 【项目计划】同步更新版方案计划到腾讯文档（项目 20 液冷超节点）
- 用户更新了方案计划（任务从 46 → **57 个**，新增 Phase2 开发阶段、顶层 7/阶段 12），按 forge-schedule-td-sync skill 模式 B（完整表重建）同步到腾讯文档子表「12-项目计划」（o99796，用户把原「10-项目计划」改名而来）。
- 执行：gen-td-full-table.mjs 生成 464 单元格（105 公式+123 数字+236 文本）→ 分批 set_range_value 写入（**每批 80 单元格**，因 Windows 命令行 32KB 限制，单次 464 会 WinError 206）→ set_cell_style 日期格式（D/E 列 1~57）→ 样式复刻（表头 E3F2FD+居中、thin 边框、顶层 7 行 FFF3E0、阶段 8 行加粗、列宽）。
- 验证：CSV 58 行全完整、公式 105 个正确（阶段 MIN/MAX、叶子 =D+F-1、前置 =MAX+1，Excel 行号 = 表内行+1）、节点任务单日日期正确；last_sync 回填 ok_full_rebuild。
- 经验补充：set_range_value 大批量（>200 单元格）命令行参数超长 → 分批（80/批）执行；结构化读回大范围会丢数字单元格显示（用 CSV 复核，新表序号纯数字无换行可靠）。
- 更新 skill 经验（批量写入分批策略）。

## GitHub 推送重试（认证失效修复后）
- 用户重连 GitHub 后，重新推送：本地提交 `68b13c0`（工作日志 2026-08-17）成功上推（`17918fa..68b13c0`）。
- 发现同步脚本产物 `server/td-values.json` 未入库但被 git 跟踪为 untracked；加入 `.gitignore` 并提交 `f5fb80b` 推送。
- 最终本地 `main` 与 `origin/main` 一致（0 ahead/0 behind，工作树干净）。
