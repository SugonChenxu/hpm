# 2026-08-17 工作日志

## 【项目计划】同步更新版方案计划到腾讯文档（项目 20 液冷超节点）
- 用户更新了方案计划（任务从 46 → **57 个**，新增 Phase2 开发阶段、顶层 7/阶段 12），按 forge-schedule-td-sync skill 模式 B（完整表重建）同步到腾讯文档子表「12-项目计划」（o99796，用户把原「10-项目计划」改名而来）。
- 执行：gen-td-full-table.mjs 生成 464 单元格（105 公式+123 数字+236 文本）→ 分批 set_range_value 写入（**每批 80 单元格**，因 Windows 命令行 32KB 限制，单次 464 会 WinError 206）→ set_cell_style 日期格式（D/E 列 1~57）→ 样式复刻（表头 E3F2FD+居中、thin 边框、顶层 7 行 FFF3E0、阶段 8 行加粗、列宽）。
- 验证：CSV 58 行全完整、公式 105 个正确（阶段 MIN/MAX、叶子 =D+F-1、前置 =MAX+1，Excel 行号 = 表内行+1）、节点任务单日日期正确；last_sync 回填 ok_full_rebuild。
- 经验补充：set_range_value 大批量（>200 单元格）命令行参数超长 → 分批（80/批）执行；结构化读回大范围会丢数字单元格显示（用 CSV 复核，新表序号纯数字无换行可靠）。
- 更新 skill 经验（批量写入分批策略）。

## GitHub 推送重试（认证失效修复后）
- 用户重连 GitHub 后，重新推送：本地提交 `68b13c0`（工作日志 2026-08-17）成功上推（`17918fa..68b13c0`）。
- 发现同步脚本产物 `server/td-values.json` 未入库但被 git 跟踪为 untracked；加入 `.gitignore` 并提交 `f5fb80b` 推送。
- 最终本地 `main` 与 `origin/main` 一致（0 ahead/0 behind，工作树干净）。

## 【项目计划】修复工期-1 与插入跳顶（feat/fix）
- **Bug1 工期写入自动-1**：根因 `addDays(date, days)` 内部已按「含首尾」减1（返回 date+(days-1) 天），但编辑/级联/导入/联动路径误传 `days-1`，导致 end 实算 start+(dur-2)，后端再按 (end-start+1) 反推工期得 dur-1。存量数据一致（end=start+(dur-1)），确认正确调用应为 `addDays(start, dur)`。
- 修复范围（同源 off-by-one）：前端 `schedule-date.js` 的 `updateStartDate`/`updateDuration`；后端 `schedule.js` 的 PUT planned_start 分支、PUT 仅改工期分支、cascade 后继 end（`addDays(start,dur)`）、import 兜底、`linkPredecessorsToStart`（前置结束=`addDays(x,0)`=x-1天）；**级联后继开始**原 `addDays(maxEnd,1)` 实算 maxEnd+0，改为 `addDays(maxEnd,2)`（=+1天）；模板生成普通任务 end 同样修正。
- **Bug2 右键插入/删除跳顶**：原 `handleInsert`/`handleDelete` 走 `loadSchedule()` 全量重载（清空调度表再回填→跳顶）。改为后端 insert/delete 返回 `getProjectTasksTree` 全量树，前端 `setTasks(applyStatus(res.data))` 原地更新，与修改时间/工期修复同源。
- 验证：前端真实函数 `updateDuration` 输入 5/14/20/1 → 存储 5/14/20/1；`addDays(07-27,2)=07-28`(+1)、`addDays(08-01,0)=07-31`(-1) 语义正确。前端 `vite build` 通过，后端 `node --check` 通过，PM2 `forge` 已重启加载新代码。
- 提交 `36df4a1`（本地），**GitHub 推送被 GCM 凭据失效阻断**（非交互 shell 无法弹浏览器重登；`git-credential-manager get` 在沙箱崩溃 exit139）。待用户在普通终端 `git push origin main` 重新授权后推送。

## 【个人空间】新增【快速排期】模块
- 用途：会议时快速搭建多轨道甘特图模拟排期；不绑定项目，按 owner 隔离。
- 数据层（db.js）新增 4 张表：`quick_schedules` / `quick_schedule_tracks` / `quick_schedule_bars` / `quick_schedule_milestones`（含 sort_order 字段、FK 级联），全部加入 `OWNER_TABLES` 自动加 owner_id 列。
- 后端路由 `server/src/routes/quick-schedules.js`：完整 CRUD + 嵌套详情（schedule → tracks → bars → milestones）；符号白名单 6 种（circle/star/triangle/square/diamond/flag），颜色 8 预设；日期自动夹紧到排期时间范围；detail 接口返回 `tracks: [...含 bars+milestones]` 嵌套结构。
- 前端 `client/src/pages/QuickSchedulePage.jsx`：
  - 时间轴以「季度+月」双层 header（图参考：暗红 #A94442 季度 / 浅红 #D9A6A5 月份），最少 6 个月；
  - 进度条绝对定位 + 拖拽平移（mousedown→mousemove→mouseup 全局监听），拖拽中保持工期不变；
  - 关键节点 SVG 内联绘制（圆/方/菱/三角/星/旗 6 种符号），拖拽改变日期；
  - 三个 Dialog：创建排期（title+起止日期）/ 编辑轨道 / 编辑进度条 / 编辑节点（双击进入）；
  - 顶部日期 chip 点击可重新设置时间范围。
- 侧边栏 `Sidebar.jsx` 加入「快速排期」入口，新增内联 `TimelineIcon`（避免 @mui/icons-material Vite 8 兼容问题）；App.jsx 注册 `/quick-schedules`。
- 验证：vite build 通过；server 重启 `curl /quick-schedules` 返回 200；`hpm.db` 验证 4 张表已建、字段齐全（milestones 含 symbol/color/date 等）。
- 提交 `c52c6f5`（本地，领先 origin 3 个提交）。推送被同一 GCM 凭据失效问题阻断，待终端 `git push` 重授权。

## 【快速排期】迭代：单排期布局 + 轨道自动箭头直线
- **需求1 去掉左侧排期列表**：同一时间仅一个项目排期，页面改为自动加载最新排期直接展示，空态引导创建（顶部始终保留「创建排期」按钮）。删除左侧列表/搜索/selectedId 相关状态。
- **需求2 创建轨道自动生成箭头直线**：
  - db.js `quick_schedule_bars` 加 `style` 列（'bar' 矩形 / 'arrow' 箭头直线）+ ALTER TABLE 迁移；
  - tracks POST 插入轨道后自动 INSERT 一条 arrow bar（start=schedule.start_date, end=schedule.end_date, color=轨道色）；
  - bars POST/PUT 支持 style；**PUT 单端点拖拽不再强制等长**（原来 `if(start&&!end)end=start` 会把另一端拉平，改为保留另一端原值、越界才跟随）；
  - `buildScheduleDetail` 给 track 加 `milestones` 数组（含未挂 bar 的独立节点），修复节点丢失 bug。
- 前端重写：
  - 新增 `ArrowBar`（左端圆点手柄 + 右端箭头 + 线体，三段拖拽：start/end/move）与 `RectBar`（矩形平移）组件，按 `bar.style` 分发；
  - 拖拽日期夹紧到排期范围（clampDate + minDate/maxDate）；
  - 节点符号下移到直线下方（top 44），箭头直线居中（top 31）；
  - EditBarDialog 加「样式」下拉（矩形/箭头直线）。
- 验证：vite build 通过；server 重启 200；`style` 列迁移成功。
- **推送成功**：用户重连 GitHub 后 GCM 缓存恢复，`f5fb80b..a48299a main -> main` 一次性上推所有滞留提交（本地=远端 a48299a，工作树干净）。

## 【快速排期】迭代：时间轴自适应 + 节点落轨道 + 顶部日期点选
- **需求1 时间轴自适应网页尺寸全量显示**：新增 `ganttRef` + `ResizeObserver` 测量甘特区宽度，月宽 = `(可用宽 - LABEL_WIDTH) / 月份数`，`MONTH_WIDTH` 常量移除，改为动态 `monthWidth` 参数贯穿 `dateToPixels`/`pixelsToDate`/`TimelineHeader`/`ArrowBar`/`RectBar`/`DraggableMilestone`/`TrackRow`；`overflowX: hidden` 全量平铺不横向滚动；月份过多以 `MIN_MONTH_WIDTH=24` 兜底；月份标签窄时只显数字。
- **需求2 节点与轨道线重合**：节点符号 top 从 44 上移到 23（18px 高，中心对齐箭头直线中线 32），拖动仅水平移动，呈现沿轨道拖动手感。
- **需求3 顶部日期可点选修改**：新增 `DateFieldPopover` 组件（Button + Popover 内 date input），放在「新增轨道」左侧，分别显示开始/结束日期，点击弹出日历选择后立即保存（`handleUpdateStart`/`handleUpdateEnd` 带起止先后校验）。
- 验证：vite build 通过；server 重启 200。
- 提交 `4d99d43`（本地，领先 origin 2 个提交：403d934 docs + 4d99d43 feat）。推送仍被 GCM 凭据失效阻断（非交互 shell 无法弹浏览器），待终端 `git push` 重授权。

## 【快速排期】迭代：节点/轨道线对齐 + 节点文字 + 轨道名快捷编辑
- **需求1 节点与箭头直线对称轴精确对齐**：统一中线 32px（ROW_HEIGHT 64 中点）。之前节点 top 23（中心 32）但箭头线体实际绝对 top 35（容器 top 31 + 线体 top 4），差 3px。改为容器 top 22：线体 top 9（绝对 31）、左端圆点 top 5（绝对 27）、右端箭头 top 3（绝对 25）、节点 top 23，四者中心均 32px 精确对齐；移除箭头直线上冗余 title 文字。
- **需求2 节点标志下方文字标注**：`DraggableMilestone` 改为「符号 + 名称」纵向 flex，容器宽 80（left-40 使符号中心对准时间点），名称 0.6rem 居中、节点色渲染、超长 ellipsis + Tooltip 全名。
- **需求3 轨道名称快捷修改**：左侧标签名称点击变 inline `TextField`（回车/失焦保存、Esc 取消）；色标点击弹 `Popover` 颜色选择器（8 预设）点选即存；删除 `EditTrackDialog` 组件及相关 `editTrack` 状态（轻量化）。
- 验证：vite build 通过；server 重启 200。
- 提交 `4867465`（本地，领先 origin 4 个提交：403d934+4d99d43+7959888+4867465）。推送仍被 GCM 凭据失效阻断，待终端 `git push` 重授权。

## 【快速排期】微调：按钮位置/字号 + 排期名快捷修改
- 「＋条」「＋节点」按钮由行垂直居中（top 50%，与轨道中线 32 重合）移到底部右下角（bottom 2, right 4），字号 0.65→0.58rem、padding 收紧，避免与箭头直线/节点重叠。
- 排期名快捷修改：顶部标题点击变 inline `TextField`（回车/失焦保存、Esc 取消），复用 `api.quickSchedules.update` 的 title 字段。
- 验证：vite build 通过；server 重启 200。
- 提交 `0079e22`（本地，领先 origin 6 个提交）。推送仍被 GCM 凭据失效阻断，待终端 `git push` 重授权。
