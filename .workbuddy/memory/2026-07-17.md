# 2026-07-17 工作日志

## OA 导入物料（Console脚本+书签+Chrome扩展 三通道，终版稳定）
- 提取脚本经 11 轮迭代，终版稳定：6条物料+型号+申请日期+内部立项号(701115)→自动匹配项目
- 关键技术决策：OA页面值在 `<input>` 元素里（非文本节点），走 label→input_field→input.value 策略
- Cookie抓取方案不适用（OA表格为AJAX动态加载，初版HTML不含物料表格）
- 三种使用方式：① Console脚本 ②书签 ③Chrome扩展（D:\HPM\chrome-extension）

## 物料管理模块全量迭代（今日改动）
- 状态卡片颜色重构：全深色字体+border；已入库蓝调(与已下单绿色区分)
- 批量编辑修复：Enter提交state滞后 + onClick传Event对象→Number(event)=NaN
- 右键菜单：底色(7色+localStorage)+删除+7字段修改
- Excel导入修复：MaterialImportDialog缺open属性+onConfirmed/onDone不匹配
- 导入映射补"总数量"/"物料描述"，日期截取YYYY-MM-DD
- 物料状态列支持排序
- 移除OA导入按钮（转由外部注入方式）
- OA导入默认"已下单"状态
- 内部立项号自动匹配项目(按order_number)→未匹配入PM项目
- "订单号"→"内部立项号"标准叫法

## 关键Bug：Vite 8 + @mui/icons-material CJS不兼容
- 根因：@mui/icons-material v5.18 CJS exports.default → Vite 8/rolldown 解析为 object → React #130 白屏
- 修复：全部替换为Unicode(▲▼▾📥+↓✕🔍↩)
- 新增 client/vite.config.js：alias 守护 x-date-pickers 子路径
