# 2026-07-16 工作日志

## 甘特图节点里程碑视觉优化（commit 5802f48）
- 用户反馈节点菱形太大 → 缩小：边长由 `barHeight+10`(32px) 改为 `barHeight-6`(约16px)，旋转45°后对角线≈条形高度，与甘特图协调
- 新增「节点标尺线」：仿「今天」线的贯穿全图竖向虚线，淡红 `#f87171`、虚线 dash、透明度0.55，顶部加小菱形标记
- 实现：`buildGanttModel` 计算节点中心线 x 数组 `nodeLines` → GanttChart 解构传参 → GanttLinks 渲染
- 构建0错误，forge 已重启，App/API 均200，已 git push

## 物料管理模块 M4 全量开发（commit 29d3d11）
- 用户给出完整 11 节规范，按规范全量实现
- 数据库：`materials` 表重建为新规范字段（旧骨架无 seed 数据，启动自动 DROP 迁移）；新增 `material_import_snapshots` 撤销快照表
- 后端 `routes/materials.js` 重写：CRUD、批量导入(连续序号+快照)、批量删除/改状态、撤销导入(GET snapshot / POST undo，5分钟窗口)、增删后全局重排序号
- 前端：`MaterialListPage.jsx` 重写——固定10列+checkbox+序号、列头排序▲▼、列宽拖拽+localStorage持久化、全字段内联编辑(失焦/回车保存、Esc取消、Tab导航)、状态彩色标签Dropdown(5态)、实时搜索、空状态、滚动保持
- 新增：`MaterialImportDialog.jsx`(xlsx解析+列名智能映射+前50行预览+错误定位)、`utils/materialExcel.js`(解析/导出带状态色)、`utils/materialStatus.js`(枚举色值)
- 安装前端依赖 `xlsx@0.18.5`（浏览器端读 .xls/.xlsx）；exceljs 仍用于导出
- 端到端 API 测试 11 项全 PASS（序号连续/快照/批量状态/重排/撤销均正确）
- 同步更新 `docs/modules/04-物料管理模块-PRD.md` 与 CHANGELOG
- 构建0错误，forge 已重启，App/API/WEB 均200，已 git push
