# 2026-07-20 工作日志

## 会议计划-输出物：周期项模型重构 + 视觉修复

### 修复的三个问题
1. **自动编号**：由「中间小号数字(width:14,0.6rem)」改为「左置前缀 `N.`，字号 0.72rem 与文字一致，加粗」，更连贯美观。
2. **周期标志**：由「标题与 ▶ 之间的小 chip」改为「标题前的连贯前缀小标签（每周/隔周/每月，带浅色底）」，不再挤压文字。
3. **设周期后无法勾选（操作失败）**：根因 `request()` 返回 `{ok,data}`，但 `handleToggleOutput` 周期分支误用 `created.id`（undefined）→ `update(undefined)` 404 抛错。

### 架构重构（关键）
- `meeting_outputs` 表新增 `is_template`(0/1) 与 `source_id` 两列；历史带 cycle 的行规整为 `is_template=1`。
- 区分「周期模板」(`is_template=1`，定义，不直接展示) 与「每周实例」(`is_template=0`，携带 cycle 供展示，source_id 指向模板)。
- 后端 GET：`outputs` 仅取 `is_template=0`；周期模板按周/隔周/每月规则生成虚拟项 `recurring_<id>`(带 `source_id`)。
- 新增 `POST /week-meetings/outputs/cycle-instance`：按 `(week_key,weekday,title)` upsert 每周实例（已存在则仅更新 is_done），避免重复行；完成态仅代表当前周期。
- `handleSetCycle`：虚拟项→更新原模板；普通项→升级为模板(`is_template=1`)。
- `handleDeleteOutput`：虚拟项删模板(停后续周期)；实例/一次性删该条。
- `handleEditOutput`：周期实例同步改模板标题。

### 验证
- 构建成功，PM2 `forge` online。
- DB 列：`id,week_key,weekday,title,is_done,sort_order,created_at,updated_at,cycle,is_template,source_id` ✓
- GET `?week=2026-07-20` HTTP200；cycle-instance 新建/更新同 id、is_template=0、source_id 保留 ✓

## 会议计划-输出物：修复「设置周期」提示设置失败

### 根因
- 数据加载函数原定义名为 `load`（第104行），但 `handleSetCycle` / `handleToggleOutput`(周期项分支) / `handleDeleteOutput` / `handleEditOutput` 均调用 `loadMeetingData(weekKey)`，该函数从未定义。
- 调用即抛 `ReferenceError`，被各 handler 的 `catch` 捕获 → 显示「设置失败」/「操作失败」。
- 为什么上次只修好普通勾选：普通项切换走乐观更新分支、不调 `loadMeetingData`，故看似正常；周期项的勾选/设周期/删除/编辑都调它，所以全报错。

### 修复
- 将定义 `const load = useCallback(...)` 重命名为 `const loadMeetingData = useCallback(...)`，并同步 `useEffect` 依赖；所有调用点统一。
- 已构建 + PM2 重启 + git 推送（commit 89083fc）。
- 教训：重命名函数时务必同步所有调用点；或保持原名 `load`。

## 会议计划-输出物：标签缩短(1W/2W/1M) + 换行对齐优化
- 周期标签文案：每周→1W, 隔周→2W, 每月→1M，更紧凑
- 行布局重构：前缀区（checkbox+周期标签+编号）固定为一块 flexShrink:0；文字区独立 Box flex:1 minWidth:0
- 多行文字换行后第二行与第一行文字起始位置对齐（不再跑到 checkbox 下方）
- alignItems 改为 flex-start，避免多行文字与短前缀垂直居中错位
- 文字点击可进入编辑模式
- 已构建+推送（commit c835269）

## 会议计划-输出物：编号并文字 + 换行对齐 1W 列（第二次微调）
- 用户要求：文字与数字序号字体大小一致；换行第二行第一个字与 1W 对齐
- 编号 N. 从固定前缀块移入文字内容（同一 Typography，0.72rem），数字不再单独 bold 块
- 左标记区仅保留 checkbox + 1W 标签；文字区 flex:1 紧接其后，换行第二行对齐到 1W 列(文字起始位)
- 已构建+推送（commit 3c90129）

## 会议计划-输出物：首行四要素同基线 + 换行对齐 1W 列（第三次微调）
- 用户要求：完成框/1W标签/编号/文字首行 水平对齐；第二行另起一行；首行始终水平对齐
- 重构：1W标签+编号+标题并入同一段 Typography(inline)，完成框作为独立 flex 项(mt:0.1rem 微调到首行中线)
- 首行四要素共享同一基线水平对齐；超宽换行时第二行落于文字块左缘(=1W列)，首行不动
- 已构建+推送（commit b5a8eed）
- 对齐迭代：flex-start+mt硬偏移 → baseline → alignSelf:center + 图标压小0.85rem（最终方案，commit 2a63363）
- 周期「每月」逻辑：isFirstWeekOfMonth(自然月1-7号) → weekDiff%4===0(每隔4周，显示仍「每月」)，commit bb2365e
- 物料管理-全选修复：原基于 rows(全部) → 改基于 filtered(筛选后可见列表)，切换仅增删筛选范围 id，commit ac66e39
- 网页挂/MIME报错根因：SPA fallback 对缺失的 /assets/x.js 返回 index.html(text/html) → 浏览器 MIME 报错。修复：带扩展名请求缺失直接 404；index.html 加 Cache-Control:no-cache。⚠️ 更正：client/index.html 实际注册了 sw.js(navigator.serviceWorker.register("/sw.js"))，之前"未注册SW"判断有误。sw.js 若存在 cache-first 可能绕过 no-cache，需警惕旧资源。commit 145c2f7
- 物料管理运行时崩溃(ReferenceError: Cannot access 'me' before initialization)：根因是 commit ac66e39 把全选块(filteredIds/allSelected/toggleAll)放在 filtered(useMemo) 之前，普通 const 渲染时提前访问 TDZ 的 filtered。修复：将全选块整体移到 filtered 声明之后。commit cfb4974
- 项目计划-修改日期 React #31(dayjs对象当子元素)：ScheduleTable DatePicker 的 dayOfWeekFormatter={(day)=>day} 返回 dayjs 对象，MUI 用它渲染日历星期表头 → #31。改为 day.format('dd')。index.html 加 mobile-web-app-capable 消弃用警告。commit c5daa6a

## PRD 全量准确性校订（2026-07-20 下午）
- 任务：依 Forge 当前实装状态检查并修订全部 PRD 文件（顶层 PRD.md + 7 个模块 PRD）。
- 核心偏差已修正：
  1. 顶层 PRD §6：M1–M6 状态「待编写」→ 全部「已落地」；M5 索引改指向 v2（tmeet CLI 版），原 v1 标废弃。
  2. 顶层 PRD §5 + M7：过时表 `week_meeting_outputs` → 现行 `meeting_outputs`（含 title/is_done/sort_order/cycle/is_template/source_id 周期模型）；M7 输出物重写为周期模板/实例模型（1W/2W/1M、自动编号、完成框、cycle-instance 接口）。
  3. M1：排期 API `/move` → 实际 `indent`/`outdent`（层级升降）；schedule_tasks 补 parent_id/notes/bg_color。
  4. M3：本地缺陷登记「待开发」→ 已实现（POST /issues source=local）。
  5. M4：OA 导入应用内按钮已移除、改 Chrome 扩展/书签/Console 外部注入。
  6. M5 v2：决议追踪/纪要编辑原列「不做」→ 实际已实现；全时会议/定时同步仍明确未做。
  7. M6：版本/aggregate 接口实际未实现；聚合仅返回精简量化指标，六板块叙述模板需手动填充。
  8. M2：看板卡片拖拽顺序原标注「未持久化」→ 已通过 PUT /tasks/:id/reorder 持久化。
- 已 git commit 并 push（PRD 准确性校订 + 当日开发日志）。

## Header 品牌重塑（2026-07-20 上午）
- Layout.jsx 左上角：原 violet 渐变 H 方块 + 「HPM 硬件项目管理」文字
  → Forge 火焰 F 图标(forge-icon-192.png, 36×36, drop-shadow 橙光)
  → 「Forge」Pacifico cursive 连笔艺术字（135°火焰渐变 橙→金→红 + drop-shadow 发光特效）
  → 「硬件项目管理」副标题(text.secondary, sm+ 可见)
- index.html 新增 Google Fonts Pacifico 字体引用
- 响应式：xs 隐藏副标题，艺术字 1.3rem→1.55rem
- commit 3ce9260，已构建推送

## Header 艺术字优化（2026-07-20 12:00）
- 用户要求：艺术字尽量斜体 + 风格与火焰图标一致
- 改动：fontStyle:italic + transform:skewX(-10deg)（强斜体）；渐变改 98° 金属火光(橙→金→亮白高光→金→红) 模拟图标 3D 金属高光；双 drop-shadow 橙金发光强化火焰能量；transformOrigin left center。
- commit fea0793，已构建推送

## 部署推广规划（2026-07-20 12:10）
- 用户目标：功能开发差不多了，要推广给同事使用，先出规划再决策执行。
- 已写 docs/DEPLOYMENT-PLAN.md（草案，待决策）。核心结论：
  - 两种形态：A 每人自托管（低改造成本、快上线、推荐起步）/ B 共享服务器（需鉴权+user_id+HTTPS+运维，大改造）
  - 换机/给同事部署断点：better-sqlite3 原生编译、Node≥20.19/22、ecosystem/start.sh 写死 D:/HPM 绝对路径、PM2 注册表开机自启失效、tmeet 个人 OAuth、Mantis/PLM 单用户凭证、OA 扩展 host、PWA 换域、无鉴权红线、无 user_id
  - 关键约束：当前无登录鉴权、所有表无 user_id（纯单用户设计）
- **决策已定（2026-07-20 12:19）**：方案 A 自托管 + 同一局域网 + 同事免装 Node + Mantis 必须/OA 选装/腾讯会议&PLM 不做。
- 规划定稿 docs/DEPLOYMENT-PLAN.md（方案 A 绿色包）：
  - 绿色包 = 内置 Node22(forge-node/) + node_modules(better-sqlite3 预编译) + 预构建 client/dist + 启动.bat(相对路径,绑 0.0.0.0:3000,打印 LAN IP) + 开机自启.bat + 备份.bat + 使用说明.txt
  - 服务器 db/dist 路径本就相对(__dirname)，绿色包可行；删 PM2 改用启动.bat
  - **Mantis 已完整实现**(路由+前端故障管理页可填 Token，存 mantis_connection 单行)，自托管天然适配，无需改造，仅文档化
  - OA 选装开箱即用(Chrome 扩展 localhost)；腾讯会议/PLM 不纳入推广
  - 局域网共享变体需谨慎(无鉴权)，可选加访问口令
  - 待用户确认是否开始 P0 制作绿色包

## 绿色包封装完成（2026-07-20 13:50）
- 用户确认"进行封装"。已交付方案 A 绿色包。
- 产出（已生成于 D:/HPM/forge-package/，161MB，被 .gitignore 忽略不进仓库）：
  - forge-node/node.exe（内置 Node 22.22.2，87MB，与现有 better-sqlite3 预编译 ABI 匹配）
  - server/（src + node_modules + package.json，不含 hpm.db 真数据）
  - client/dist/（已构建前端）
  - 启动.bat / 备份.bat / 开机自启.bat / 防火墙放行.bat / 使用说明.txt
- 可版本化源码模板：scripts/portable/*（避全局 dist/ 忽略，故命名 portable）+ scripts/package.bat（组装脚本）。commit 61ace4c 已推送。
- 冒烟测试（PORT=3001，避免打扰 3000 生产实例）：首页200、/api/projects 返回 ok（空库自动建表+种子）、/api/mantis/connection 正常、静态资源相对路径生效、不依赖全局 Node/PM2/D:/HPM。**验证通过**。
- 注意：测试库 hpm.db 因 Windows 文件锁未能删除，但仅为空种子库（无用户数据），随包分发无碍。
- 同事使用：解压 forge-package/ → 双击 启动.bat → 开 http://localhost:3000；Mantis 在故障管理页配自己 Token；OA 选装。
- 后续若改代码：重跑 scripts/package.bat（或 bash 组装）重新生成 forge-package/，压缩分发。

## 绿色包新增 OA 导入脚本（2026-07-20 14:20）
- 用户要求：把 OA 书签脚本放进 forge-package 文件夹，并在使用说明.txt 写清配置方法。
- 已放入 forge-package/OA导入/：书签链接.txt（javascript: loader，加载 http://localhost:3000/oa-bookmarklet.js）+ OA导入脚本.js（v4 完整浮窗版，Console 用）+ OA浮窗脚本.js（loader 实际加载的脚本副本）+ 配置方法.txt（精简指引）。
- 源码：scripts/oa-bookmarklet.txt、scripts/oa-import-bookmarklet.js、client/public/oa-bookmarklet.js（均已在 git 跟踪，package.bat 可复现）。
- scripts/portable/使用说明.txt 第五节由"另行提供"扩展为书签方式(推荐)+控制台方式(备用)两步法，含端口变更提示(改 PORT 需同步改脚本里 localhost:3000)与表头识别前提(物料编号/型号/数量)。
- scripts/package.bat 加步骤6.5：重打包自动复制 OA 三脚本到包内 OA导入/。
- 验证（内置Node@3001）：/oa-bookmarklet.js 返回200(书签loader能加载真实浮窗脚本)；POST /api/materials/oa-import 返回200(空items正常)。证明同事书签导入链路可用。commit 92b7163 已推送。

## 推广形态重大转向（2026-07-20 14:37）
- 用户权衡「绿色包每人本机 vs 内网网页」后决策：**走内网网页集中部署 + 各自独立管理**。
- 决策：内网网页（集中一份，同事浏览器访问，零安装）+ 数据各自独立（登录 + 全表 owner_id 隔离，无角色权限）。
- 推翻 12:19 定的「方案A 绿色包每人本机」。需做多用户改造（认证 + 数据隔离），属中等偏大全栈改造，分模块串行。
- 前端已用相对路径 /api（client.js `BASE="/api"`），局域网网页几乎免前端 host 改造。

## 多用户改造细化决策收口（2026-07-20 14:55）
- 用户回答四个落地细节，决策全部落定：
  1. **先建专属账号**：为本人建 chenxu 账号，将 D:/HPM 现有全量数据 migrate 绑定到该 owner_id（避免登录后空白）。
  2. **本机即服务器**：就用你办公电脑常开跑 Forge，不另购/另找独立服务器；数据先放本机。
  3. **手动逐个添加**：同事不多，账号不开放注册，提供建账号脚本逐个建。
  4. **登录尽量长**：Session 时效取最长（30 天或持久记住我），减少同事重复登录。
- 补充（前序已定）：认证=Session；外部对接 Mantis 必须/OA 选装/腾讯会议+PLM 不做；演进=先本机试用→后整包迁移内网服务器（SQLite 单文件拷贝即迁移）。
- 已固化进 MEMORY.md「用户决策」区块 + DEPLOYMENT-PLAN.md「已决策项（含细化）」。决策收口，待用户确认开工阶段0（认证基础设施）。
- 绿色包 forge-package/ 定位转为「服务器部署包」（解压到常开机器常驻），同事侧只需浏览器+网址。
- 待：写多用户改造执行计划（阶段0认证→1数据隔离→2前端登录→3外部对接适配→4部署→5验证），用户确认后开工。

## 多用户隔离改造 — 已完成并验证（2026-07-20 15:58）
- 按"按照你的计划执行"完成全栈多用户隔离改造，commit 767328c 已推送 main（28 文件，+1242/-263）。
- 阶段0 认证基础：users 表 + 12 张业务表 owner_id 增量迁移(db.js)；auth.js(login/me/logout, bcryptjs)；auth-middleware.js(requireAuth, 白名单 /materials/oa-import|oa-fetch, OPTIONS 放行)；index.js(Session 30天文件存储 + 路由挂载顺序)。依赖 express-session/session-file-store/bcryptjs。
- 阶段1 路由 owner 过滤：projects/tasks/issues/materials/meetings/weekly-reports/week-meetings/schedule/mantis 全加 owner_id；materials/oa-import 无会话时按项目 owner_id 回填；weekly-reports 全项目汇总分支也加 owner_id；plm 保持全局共享(去推广范围)。
- 阶段2 前端登录：AuthContext + LoginPage(内联 SVG 图标避开 @mui/icons-material) + App 路由守卫(未登录跳登录) + Layout 顶栏当前用户/登出；main.jsx 包 AuthProvider。
- 迁移脚本：server/scripts/create-user.js(手动建/重置账号) + server/scripts/bind-legacy-data.js(chenxu 绑定 182 行历史数据)。
- 端到端验证：未登录 GET /api/projects→401；chenxu 登录→6 项目可见；双账号(alice)隔离测试通过(后清理)。生产构建成功，PM2 forge online，http://localhost:3000/ 返回 SPA。
- 初始账号 chenxu 密码由迁移脚本随机生成，登录后可经 create-user.js --reset 修改；同事开号用 create-user.js <用户名> <密码>。
- .gitignore 增补 server/src/sessions/、server/backups/、forge-package.zip。
- DEPLOYMENT-PLAN.md 新增 §6 账号管理与迁移实操（含初始登录/开号/重置密码/隔离说明）。

## 故障管理(M3) Mantis 同步修复 + 每用户 Cookie（2026-07-20 17:49）
- **现象**：多用户改造后 chenxu 拉不动 Mantis 数据。
- **根因（两项）**：
  1. `fetchIssues` 方法在 SSO 重构 commit 5b110df 被误删 → `POST /mantis/sync` 抛 `TypeError: adapter.fetchIssues is not a function` → 500；issues 表 0 行。
  2. 适配器 `constructor` 读**全局** `mantis_connection LIMIT 1`，未用当前用户 cookie；且路由把 Forge **数字** project_id 直接传给需要 Mantis **hex** id 的接口 → 全部拉空/报错。
- **修复**：
  - 新增 `server/src/mantis-resolve.js`：`getAdapter(ownerId)`（按用户建适配器，无 cookie 抛 auth_failed）、`resolveMantisId(ownerId, forgeProjectId)`（优先用 `project_mapping`，缺失时按项目名模糊匹配并**自动写回映射**）、`mantisError(e, fallback)`（错误码→HTTP+中文）。
  - `adapters/mantis.js`：`constructor(conn)` 改为每用户；`_req` 包裹归一化错误码(auth_failed/timeout/network)；`fetchProjects()` 返回**全部**项目（去掉硬编码关键字过滤）；保留已还原的 `fetchIssues(POST /my/views)` 与 `fetchAllProjects()`。
  - `routes/mantis.js`：`/mantis/projects`、`/mantis/sync` 改用每用户适配器+解析器；同步字段映射改为中文 severity/status 原样存、`di_weight` 优先用 `issue.di_value`、`mantis_id=issue.issue_id`、`assignee=issue.handler`、`category` 数组 join；`/cache/invalidate` 加 owner_id。
  - `routes/issues.js`：di-trend/category-stats/summary/report 四个聚合端点改用每用户适配器+`resolveMantisId`，并对 Mantis 错误做状态码映射。
  - 前端 `components/issue/MantisConnectionCard.jsx`（新增）：故障仪表板顶部「Mantis 连接配置（每用户独立）」卡片，填 server_url + Cookie 即 PUT /mantis/connection，保存后刷新项目下拉；`IssueDashboardPage.jsx` 挂载该卡片。
- **关于「最近使用项目」**：经 puppeteer 抓包证实 Sugon Mantis **无**「最近使用项目」接口，`/projects` 仅返回全部 40 个项目；故下拉展示全部，选择后自动按名匹配并记录映射。已在前端卡片注明。
- **验证**：node 级 verify 通过——chenxu cookie 仍有效；`resolveMantisId(6)`→`695a1917425fc624d2eb6927`；`fetchIssues` 返回 128 条；upsert 入库 0→128，样本字段(普通问题/已解决/di_weight/category/mantis_updated_at)正确。HTTP 路由挂载+鉴权确认(401 非 404)。生产构建成功，PM2 forge online。
- 清理临时探针脚本(_probe*/_checkmatch/_verify*/capture-mantis/find-recent/postbody.txt)。
- 注意：pm2-error.log 偶发 `EPERM rename sessions/*.json`(session-file-store Windows 文件锁竞态)，与本次无关，真实登录正常。

## 故障管理(M3) 403「无权访问该项目」二次修复（2026-07-20 18:30）
- **现象**：上一轮修复后 chenxu 在故障仪表盘仍报「无权访问该项目」(403)。用户给密码 TCB712sj#33 要求用真实 Web 端验证。
- **根因**：前端故障仪表盘下拉(`RefreshBar.jsx`)用的是 `/api/mantis/projects` 返回的 **Mantis hex id**，但后端 `POST /mantis/sync` 与 4 个聚合端点(`/issues/{di-trend,category-stats,summary,report}`)在归属校验处 `SELECT id FROM projects WHERE id=? AND owner_id=?` 期望的是 **Forge 数字 id** → hex id 匹配不到 Forge 项目 → 403。而缺陷列表页 `IssueListPage`/`GET /issues` 用的是 Forge 数字 id（`ProjectSelector` + `Number(projectId)`），本就正确，未受影响。
- **修复**：新增 `resolveForgeId(ownerId, mantisId)`（逆向解析：优先查 `project_mapping` 的 mantis_id→forge_id，缺失时按项目名模糊匹配并自动写回映射），在路由边界把前端传入的 **Mantis hex id → Forge 数字 id**，归属校验与落库/缓存都用 Forge id，适配器拉取用 Mantis hex id 原值不变。
  - `routes/mantis.js` `/mantis/sync`：先 `resolveForgeId` 得 forgeId 再做归属校验；`insert.run(forgeId,...)`；缓存删除改用 forgeId。
  - `routes/issues.js`：4 个聚合端点同样先 `resolveForgeId`，归属校验用 forgeId，缓存 key 改用 forgeId。
- **多用户隔离补强**：`sync_cache` 表原无 `owner_id`，同号项目会跨用户共享缓存(300s) → 新增 `owner_id` 列(ALTER)+ 索引含 owner_id；`getCache/setCache` 增加 ownerId 参数；`/mantis/sync` 的缓存删除本就带 owner_id。
- **Web 端验证（chenxu 真实登录 TCB712sj#33 全链路）**：登录 200 → `/api/mantis/projects` 200(40项) → `/api/mantis/sync` 200(sync_count 104) → summary/di-trend(14点)/category-stats(11行)/report 均 200 真实数据 → `/api/issues?project_id=6` 200(128条)。**403 彻底消失**。
- **映射正确性核验**：两个 HG 9400 项目按后缀正确区分——`695a1917425fc624d2eb6927`(勒拿河)→Forge#6(整机柜液冷节点服务器)128条；`695a19ac83016254bb1ef331`(马泉河)→Forge#7(高性能双路机架服务器)104条。
- 已 commit d9014fc 推送 main，PM2 forge online。临时脚本 `_verify_web.mjs` 已清理。

## 故障管理(M3) — 关注项目 + 手动映射 + 配置隐藏（用户二次反馈）
- 用户反馈：①下拉显示全部 40 项太多，只要「最近使用的项目」；②自动名称模糊匹配的对应关系不对；③Cookie 设好后配置卡片应隐藏，点按钮才显示。
- **Sugon Mantis 确认无「最近使用项目」API** → 改为 Forge 侧建模「用户关注项目列表」`watched_projects`。
- `db.js`：mantis_connection 新增 `watched_projects TEXT`（ALTER 迁移）。
- `mantis-resolve.js`：新增 `getWatchedProjects(ownerId)`（老用户 project_mapping 自动升级为 watched_projects 并补全名称，仅一次）；`resolveForgeId` 改为**仅读 watched/project_mapping 反查，不再做名称模糊匹配自动写回**（避免错配），找不到明确报 422 引导去设置。
- `routes/mantis.js`：新增 `GET /mantis/watched-projects`；`PUT /mantis/connection` 持久化 `watched_projects`。
- 前端：`MantisConnectionCard` 重构为「Cookie + 手动勾选关注项目(搜索过滤) + 每项选对应 Forge 项目」；`IssueDashboardPage` 加「⚙ Mantis 设置」按钮，卡片默认隐藏，下拉只显示关注项目（空时引导去设置）；刷新时自动同步一次。
- **Web 验证**：watched-projects 返回 3 项（勒拿河#6/马泉河#7/海南岛_星载计算机#21），全部 Mantis 仍 40（供设置卡片选）；同步 128；未关联项目同步→422 清晰提示。
- 已 commit 0f8f996 推送 main，PM2 forge online。

## 故障管理(M3) — 改用 Mantis 真实「最近使用项目」接口（用户指正抓包有误）
- 用户指正：之前"无最近使用项目接口"的结论是**错的**。用真实 cookie 重新抓包确认：主页 JS `reload_recent_projects()` 调 `GET /projects?action=get_recent_projects`，渲染「最近使用的项目」(实为项目集合列表)。之前误用 POST 探测才 500。
- 真实接口：`GET /projects?action=get_recent_projects` → `{data:[{id, projects:[{id,name}...]}...]}`；每集合通常 1 个项目。chenxu 实测返回 **9 个**最近使用项目。
- **架构调整（仪表盘改为 Mantis 原生，不强制 Forge 映射）**：
  - `adapters/mantis.js` 新增 `fetchRecentProjects()`（GET + 扁平去重）。
  - `mantis-resolve.js` 新增 `getRecentProjects(ownerId)`。
  - `routes/mantis.js` 新增 `GET /mantis/recent-projects`（真实接口）；保留 `watched-projects` 兼容。
  - `routes/issues.js` 4 个分析端点改为 **Mantis 原生**：`project_id` 即 Mantis hex id，直接用适配器拉数据，**去掉 resolveForgeId 与 Forge 归属校验**（不再有"对应关系不对"问题）；缓存按 mantis id + owner_id。
  - `db.js`：`sync_cache.project_id` 由 INTEGER 改为 **TEXT**（需同时存 Forge 数字 id 与 Mantis hex id），含存量迁移。
  - 前端：`IssueDashboardPage` 下拉改 `recentProjects()`，移除自动同步/刷新同步（仪表盘即实时数据）；`MantisConnectionCard` 改为"列出最近使用 Mantis 项目→逐项关联 Forge 项目"（仅用于缺陷同步进 Forge 列表），来源为真实 recent 接口。
- **Web 验证(chenxu 真实登录)**：登录 200 → recent-projects 9 项 → summary 128/解决率71.88%/DI7.62、di-trend 29点、category-stats 5行、report 122字，全部 200。
- 已 commit（本次）推送 main，PM2 forge online。
