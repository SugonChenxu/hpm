# 2026-07-13 工作日志

## 开发/生产双模式配置
- 用户需求：封装完成后继续开发新功能，需要恢复热更新开发环境
- 实现：
  - 新增 `ecosystem.dev.config.js`：双进程开发模式（forge-api:3001 + forge-ui:5173）
  - 更新 `start.sh`：新增 `dev`/`build` 命令
    - `bash start.sh dev` → Vite HMR 5173 + Express API 3001
    - `bash start.sh build` → npm run build + 切生产模式 3000
  - 生产模式 `ecosystem.config.js` 不变（单进程 forge:3000）
- MEMORY.md 同步更新 PM2 管理命令文档

## WorkBuddy ↔ Forge 互联验证：腾讯文档 → 项目导入
- 读取腾讯文档「S2000项目进度计划」→ CreateProjectDialog 已有 useProjectContext → Api创建项目 + Node脚本导入28个排期节点（10阶段+18子任务）
- 写入 Node 脚本 `scripts/import-s2000.js`（better-sqlite3 直接操作 hpm.db）
- 验证：项目 ID=13 创建成功，API 正常返回

## 新建项目颜色修复：固定紫色 → 10 色调色板轮换
- 问题：`existingCount` 参数无人传递，永远取 `PALETTE[0]` = 紫色 #8B5CF6
- 修复：`CreateProjectDialog.jsx` 第 37 行从 `useProjectContext` 获取 projects，取色改为 `PALETTE[projects.length % PALETTE.length]`
- 现有 5 个项目：下一个新建项目→蓝色(#3B82F6)，再下一个→深紫(#6D28D9)，以此类推循环 10 色
