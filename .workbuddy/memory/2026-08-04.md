# 2026-08-04 工作日志

## 运维：服务器挂了，按 SOP 情况A 恢复
- 现象：`pm2 status` 进程列表空，`http://localhost:3000` 返回 000。
- 根因：PM2 daemon 未运行（daemon 进程退出 / 重启后未拉起），非代码问题。
- 处理：`npx pm2 start ecosystem.config.js`（forge 单进程生产模式）→ `npx pm2 save` 固化 dump.pm2。
- 复查：Client 200、API 401（路由正常仅鉴权），服务已恢复。
- 自启机制复核：注册表 `HKCU\Run\HPM-PM2-AutoStart` → `scripts\pm2-resurrect.bat` 路径有效（`node_modules\pm2/bin/pm2` 存在，v7.0.3），下次开机应可自动 resurrect。
- 备注：若日后频繁出现 daemon 自行退出，需进一步排查退出原因（内存/OOM/系统策略）。
