# 2026-07-09 工作日志

## BugFix: 待办响应式 + PM2 进程守护

### 待办列表不立即更新 Bug
- **根因**: `TodoColumn.jsx` 在 render 阶段调用 `setLocalTasks`，React 18 并发模式下不可靠
- **修复**: 替换为标准 `useEffect([tasks])` 监听 props 变化同步 localTasks
- **文件**: `client/src/components/kanban/TodoColumn.jsx`

### PM2 进程守护（根治网页挂掉）
- **根因**: node 进程附着在 WorkBuddy bash 会话上，会话超时而死
- **方案**: PM2 进程管理器 — 崩溃自动重启、脱离终端、保存进程列表
- **配置**: `ecosystem.config.js` — hpm-server + hpm-client
- **管理**: `bash start.sh [start|stop|restart|status|logs]`
- **状态查看**: `npx pm2 status`
- **文件**: `ecosystem.config.js`, `start.sh`, `start_backend.bat`

### M5 会议纪要模块
- **方案**: tmeet CLI child_process 调用 → SQLite 存储 → MUI Table + Drawer
- **核心链路**: 拉取按钮 → tmeet meeting list-ended → 入库 → 表格展示 → 点击查看 AI 纪要
- **数据**: 42 场会议拉取成功，按 external_id 去重
- **已知**: tmeet auth status 返回纯文本非 JSON（已修复 checkAuth 逻辑）
- **文件**: `server/src/adapters/tencent-meeting.js`, `server/src/routes/meetings.js`, `client/src/pages/MeetingListPage.jsx`, `client/src/components/meeting/MeetingDrawer.jsx`, `client/src/components/meeting/SmartMinutesViewer.jsx`

### BugFix: react-dom 包损坏导致页面转圈打不开
- **根因**: `node_modules/react-dom/cjs/react-dom.production.js` 文件缺失（包损坏），Vite 依赖优化反复失败，客户端进程崩溃-重启循环（↺ 11+ 次）
- **修复**: `npm install react-dom react` 重装 + 删除 `node_modules/.vite` 缓存 + `pm2 restart hpm-client`
- **验证**: main.jsx 模块正常解析，react/react-dom/MUI 依赖成功打包，客户端 uptime 稳定

### BugFix: Vite --force 无限循环 + fastRefresh:false 导致白屏
- **根因1**: `ecosystem.config.js` 的 `--force` 标志导致 Vite 每次启动强制重新优化依赖 → 优化中崩溃 → PM2 重启 → 又 --force → 死循环
- **根因2**: `@vitejs/plugin-react` v6 设 `fastRefresh:false` 后仍注入 `$RefreshSig$()` 但不加载 Refresh 运行时 → ReferenceError → 白屏
- **修复**: 移除 `--force` 标志 + `NODE_ENV` 改为 development + 移除 `fastRefresh:false`（恢复默认 Fast Refresh，HTML 正确注入 /@react-refresh preamble）

### 改版迁移：项目概览页面改版（从待办事项迁移到项目概览）
- **问题**: 之前的"项目看板大改版"被错误应用到了 TaskKanbanPage（待办事项），用户需要的是 DashboardPage（项目概览）
- **操作**: 回退 TaskKanbanPage 到原始版本 + 重写 DashboardPage 使用 ProjectCard 组件
- **DashboardPage 新功能**: 彩色边框(theme_color) / 右键编辑(CreateProjectDialog 编辑模式) / 信息聚合(订单号/库位/例会时间/当前阶段) / 待办摘要(前3条) / dnd-kit 拖拽排序
- **ProjectCard 修复**: `MenuProps` → `PaperProps`（MUI v5 Menu 组件不接收 MenuProps）
- **提交**: e88429b 已推送到 origin/main
