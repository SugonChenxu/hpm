# 2026-07-09 工作日志

## BugFix: 待办响应式 + 服务稳定性

### 待办列表不立即更新 Bug
- **根因**: `TodoColumn.jsx` 在 render 阶段调用 `setLocalTasks`（第21-29行），React 18 并发模式下 render-phase setState 不可靠
- **修复**: 替换为标准 `useEffect([tasks])` 监听 props 变化同步 localTasks
- **文件**: `client/src/components/kanban/TodoColumn.jsx`

### 服务反复挂掉
- **根因**: bash 会话超时时子进程（nohup node）被连带杀死；Windows Git Bash 不支持 `setsid`
- **修复**: `start.sh` 改用 `nohup` + 直接 node 路径（不用 npx）
- **可靠启动方式**: `bash /d/HPM/start.sh`
- **文件**: `start.sh`, `start_backend.bat`, `start_fe.bat`
