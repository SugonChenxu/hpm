# 2026-08-19 工作日志

## 项目计划：前置联动日期偏差修复（关键）
- 现象：后置任务开始时间不是「前置任务结束时间 + 1 天」（实际 +2 天）。
- **根因**：后端 `schedule.js` 的 `addDays(date, n)` 语义是「+n 天」，而前端 `schedule-date.js` 是「+n-1 天」（含首尾）。前后端不一致 → 后端所有日期换算多 1 天：反向联动「前置结束 = 后置开始 - 1」被算成「= 后置开始」(`addDays(开始,0)`)、正向级联「后置开始 = 前置结束 + 1」被算成「+2」(`addDays(结束,2)`)。
- **修复**：后端 `addDays` 改为含首尾语义（`date + n - 1`，与前端一致），全部 14 处调用点按其意图参数自动正确。已验证：真实数据联动后 B 结束 = A 开始 - 1 ✓。
- **历史数据**：`server/fix-predecessor-dates.mjs`（dry-run 默认 / `--apply` 执行）级联修正「后置开始 ≠ 前置结束+1」的任务，扫描出 23 个（液冷超节点模板链 L5→原理图→Layout→PCB→PCBA→PowerOn→测试 层层 +1）。**执行前需用户确认**（可能收紧手动排期的空档）。
- 教训：**前后端日期工具函数必须语义一致**（含首尾约定）。后端 addDays 注释已补示例（`addDays("2026-08-21",5)="2026-08-25"`、`addDays(A开始,0)=A开始-1`、`addDays(B结束,2)=B结束+1`）。
- **执行结果（10:26）**：用户确认后 `node fix-predecessor-dates.mjs --apply` 级联修正 23 个任务全部成功；复扫 0 残留；抽查 L5 开始 8/28 = L4 结束 8/27 +1 ✓、PowerOn 1/18 = PCBA 结束 1/17 +1 ✓。db 文件被 gitignore，无需提交。

## 排期日期语义切换：不含首尾（完成 = 开始 + 工期）
- 用户反馈「完成时间 = 开始时间 + 工期，现在少了一天」→ 明确要求**不含首尾**语义（工期 = 结束 - 开始）。
- **最终约定（2026-08-19 定稿）**：完成 = 开始 + 工期；工期 = 结束 - 开始；前置联动 B 结束 = A 开始 - 1；A 开始 = B 结束 + 1。节点任务单日（开始=结束）、阶段任务聚合（工期 = 结束 - 开始）。
- 改动：后端 `schedule.js` addDays 标准 +n、daysBetween 结束-开始，调用点 `addDays(开始,-1)`/`addDays(结束,1)`/`addDays(结束,-工期)`；前端 `schedule-date.js` 同步；`scheduleMapping.js`（导入推导 `+d`/`-d`）、`scheduleExport.js`（Excel 公式 `=开始+工期`）同步。
- 迁移：`server/migrate-duration-semantics.mjs`（dry-run/--apply，**嵌套阶段聚合需多轮收敛 + byId 同步**）执行 141 项，复扫 0 残留；全部普通任务 工期 = 结束 - 开始 一致；联动验证 B 结束 = A 开始 - 1 ✓。
- 教训：日期语义变更需**前后端 + 导入导出 + 迁移脚本**全链路同步；addDays/daysBetween 已统一「不含首尾」并在注释固化示例。

## 快速排期：导出 Excel + 打通腾讯文档（新功能）
- 新增「导出 Excel」按钮（`client/src/pages/QuickSchedulePage.jsx`），生成双表 xlsx：①「排期明细」全量结构化数据（轨道/进度条/箭头/节点/参照线，颜色填充+自动筛选）②「甘特图」可视化（日期轴+彩色进度条填充+节点●+参照线▼，冻结窗格、月度分色）。日期语义沿用不含首尾。
- 新增 `server/src/quick-schedule-xlsx.js`（ExcelJS）+ `GET /api/quick-schedules/:id/export/xlsx` 路由（与 PPT 路由同构）。
- **腾讯文档实测打通**：用已连接连接器 `import_file.py`(pre_import+COS PUT) → `manage.async_import` → `manage.import_progress` 轮询，成功把「项目规划」排期导入为在线表格 `https://docs.qq.com/sheet/DUXp5cFBnZEtjUHBI`(file_id=QzypPgdKcPpH)。云端推送当前由连接器(agent)执行，因腾讯文档票据由宿主注入、不在局域网服务器持有；同事可用「导出 Excel」下载后拖入腾讯文档。
- **ExcelJS 坑（已踩）**：solid 填充必须写 `{type:'pattern', pattern:'solid', fgColor:{argb:'FFxxxxxx'}}`（只写 `type:'solid', color:...` 不生效，readback 也读不到）。已沉淀为 skill `forge-excel-export`。
- 已提交推送 57568b3，生产构建通过、forge 已重启(在线)。

## 快速排期：导出 Excel 改为「形状版可视化甘特图」（18:19 重构）
- 用户澄清：要的是和 PPT 一样**可拖动的视觉甘特图**，不是填色表格/数据明细表。
- 重构 `server/src/quick-schedule-xlsx.js`：ExcelJS 生成「日期网格+轨道标签」单表骨架 → jszip 注入 `xl/drawings/drawing1.xml`（`spreadsheetDrawing`，twoCellAnchor 单元格锚定）把对象变成**真正的 Excel 形状**：进度条=圆角矩形(可拖动/改大小,内嵌白名)、箭头=细条、节点=菱形(按 symbol 映射 circle/square/triangle/star/flag)+名称文本框、参照线=贯穿竖条+名称框。每日一列、月度交替底色、不含首尾语义。
- 注入要点：worksheet 加 `<drawing r:id="rId1"/>`(补 xmlns:r)、`_rels/sheet1.xml.rels` 加 drawing 关系、`[Content_Types]` 加 drawing override；drawing 内用 `<xdr:wsDr>`(spreadsheetDrawing ns) + `<a:>`(drawingml ns)。已用 Python xml 解析器校验 sheet/drawing/rels/ct 全良构。
- **关键限制（已同步用户）**：腾讯文档在线表格是独立渲染引擎，**不保留 Excel drawing 形状**，导入后只剩日期网格、看不到可拖动进度条；真正可拖动视觉甘特图必须用 **Excel/WPS 打开导出的 .xlsx**（与 PPT 同理）。本地交付文件 `D:/tmp/项目规划_快速排期_可视化.xlsx`。
- 已提交推送 db6a176，生产构建通过、forge 在线。

## 腾讯文档甘特图「月度示意版」（一格一月，2026-08-19 晚）
- 需求：另开新表 docs.qq.com/sheet/DUU9xZ3RWdHV2ZHdJ，横向季度+月份（一个月一格）、纵向轨道名，形状还原，不逐日。已建成：15 个月列(130px)、季度合并、7 条贯穿箭头线、3 短进度条、18 菱形节点(按日份月内定位)、2 全高参照线+列染色保险、冻结+行高列宽。
- **关键坑（务必记）**：腾讯文档**同 (row,col) 锚点重复 insert_image = 后者替换前者**（探针确认，两次都返回成功但导出只剩 1 张）。布局阶段必须规避锚点冲突；冲突修复法=里程碑改插相邻空闲锚点+PNG 加宽+菱形按目标列偏移绘制，进度条回原锚点后插。
- 残留清理：无删除图片 API，残留图用「全透明 PNG 覆盖」隐藏（导出仍列出透明对象，UI 不可见）。
- 复用：高图(vline)/大量图片走直连 HTTP insert_image（token 经 connector-proxy /internal/tencent-docs/tokens）；导出核验=manage.export_file+export_progress 下载 xlsx 解析 drawing1.xml 的 oneCellAnchor from/col,row。脚本 D:/tmp/_build_tdoc_gantt_month.py / _fix_month_collisions.py（一次性，未入库）。

## 腾讯文档甘特图 V2：每格一段拼接（轨道线+参照虚线，2026-08-19 19:32）
- 根因：腾讯文档**单元格锚定图片被等比缩放到格内**（探针：130×40→123×37.8 占满格；1942×30→123×1.9 塌成细条）→ 跨格单图必塌。用户反馈「只在一个单元格内画线」。
- API 结论：sheet-mcp 42 工具**无矢量形状 API**；insert_image 无尺寸参数；Web UI 可手动插形状但不可自动化。
- 方案：每格一张 130×40 拼接图（渲染占满格），相邻格连成跨格线。合并规则：轨道线(色+末列箭头)+参照虚线(红,列9/10 每行一段)+短进度条(圆角分段,名称放首无节点格)+里程碑(菱形+9px标签,日份定位) 按格合绘；旧塌缩图同锚点自动覆盖。
- 验证：105 段每格 1 张全 123×37.8px。脚本 D:/tmp/_build_segments.py（一次性）。

## 腾讯文档甘特图：原生形状结论 + 干净画布（2026-08-19 19:35）
- 用户要求：不用图片，用「插入形状」原生矢量对象，表格内可自由编辑。
- **最终结论**：sheet-mcp 42 工具无形状 API；形状工具只在 slide-mcp（幻灯片）；insert_image 无矢量参数；无浏览器自动化技能 → API 无法自动插原生形状；Web UI 手动「插入→形状」可行。
- 落地：manage.create_file 新建干净表（file_id=QoJHMpVlfwkI，URL DUW9KSE1wVmxmd2tJ），只搭布局零图片；交付手动形状清单（7 轨道箭头 B4→P4..B10→P10、2 虚线 J4-J10/K4-K10、3 圆角矩形、18 菱形节点坐标配色）。
- 误建空白重复文件 QpYhZnuDuKEC，提示用户删除。
- 用户决策：选「手动插入 + 精确清单」，放弃浏览器自动化与图片版。

## 本地 Excel = 原生形状版甘特图（2026-08-19 19:45）
- 用户问「本地 excel 能不能插形状」→ 能，且 HPM「导出 Excel」早已实现：生成 .xlsx 的 drawing1.xml 含 48 个 twoCellAnchor **原生矢量形状**（<xdr:sp>，pics=0，非图片）——进度条圆角矩形/箭头细条/节点菱形/参照线竖条，Excel/WPS 打开即可自由拖动缩放改色。
- 结论与腾讯文档对比：**本地 Excel 原生形状可自由编辑（满足用户诉求）；腾讯文档 API 只能插图片（格内缩放、不可编辑）**。给用户交付 D:/tmp/项目规划_快速排期_可视化.xlsx（重新生成于本会话，14740B）。
- 复用：无登录生成导出=node 脚本直接 import buildScheduleXlsx(detail)，detail 结构见 quick-schedules.js buildScheduleDetail（schedule+tracks(bars,milestones)+vlines）。

## 「导出 Excel」月度示意版（一格一月+原生形状+美化，2026-08-19 19:50）
- 用户拍板：月度示意版做在**本地 Excel**（原生形状可自由编辑）。
- 重构 quick-schedule-xlsx.js：月度网格（15 月列×22字符）、季度合并蓝系表头、月份一格一月交替色、轨道标签彩色块、图例行、冻结4行+1列；轨道线=line+tailEnd箭头、参照线=line+dash虚线、进度条=roundRect白粗体名、节点=symbol映射+名称框。
- 验证：48 twoCellAnchor（7箭头线+3进度条+18节点+18名框+2虚线），pics=0，XML 全良构；生产重启 forge 在线；页面「导出 Excel」已生效；本地文件 D:/tmp/项目规划_快速排期_月度示意_可视化.xlsx。

## 导出Excel月度版微调：对齐Forge样式（2026-08-19 20:10）
- 参考 Forge 常量：LABEL_WIDTH=160px→轨道列22字符；月宽~96px→13字符；节点size=18→固定18px；行高64px→32pt。
- 关键修复：节点锚点原来 colTo=c+1+28%~72%偏移实际宽=1.44×月列(~229px) 过大 → 改同格锚点(colFrom=colTo=c，仅colOff定位) 精确18px，名称框收窄 c+1..c+2。
- 验证：18节点全部18px、48形状结构不变、XML良构、生产重启。文件 D:/tmp/项目规划_快速排期_月度示意_V2.xlsx（预览路径同步）。

## 导出Excel V3：拉直+简化配色+Forge比例定位（2026-08-19 20:18）
- ①线条不直根因：line 形状盒对角（from/to 的 rowOff/colOff 不等）→ 修：箭头线 rowOffFrom==rowOffTo、参照线 colOffFrom==colOffTo（绝对水平/垂直），核验 not-straight=0。
- ②配色：去掉轨道区交替底色带；月份表头单色浅底深字；节点名从居中改左对齐紧随符号；文字深灰 #374151；形状颜色直接输出数据色。
- ③Forge 比例：dateToPixels = 月序号×月宽 + (当月已过天数/当月总天数)×月宽；节点中心对准日期点（colOff=frac*CELL±w/2，clamp≥0），进度条左=开始日右=结束日，参照线=日期点竖线。18 节点中心 px 逐一吻合公式。
- 产出 D:/tmp/项目规划_快速排期_月度示意_V3.xlsx，生产重启，commit 50b0a93 后新提交。

## 导出Excel V4：全面对齐PPT导出（2026-08-19 20:40）
- 关键根因（重要）：ExcelJS 传 6 位色值 "1F2937" 会写 color auto="1"（自动=黑）→ 深底黑字不可见（用户报"标题黑字看不见/很多字看不清"）。修复：字体色全部显式 {argb: argb("FF...")}。
- PPT 配色落地：季度红 A94442/月份浅红 D9A6A5 白字、月带白/EEF2F7、轨道名浅底纹+深字374151+左粗边、进度条8pt白字白边、节点15px文字下方居中7.5pt、行高36pt。
- 布局：删第二行副标题（标题/季度/月份/轨道，冻结3行）；A2/A3 角格留空；轨道箭头右端=最后月右侧（colTo=16,colOffTo=0）。
- 核验：48形状、pics=0、线条0斜、18/18标签在符号下、字体ARGB正确。产出 V4 文件。
