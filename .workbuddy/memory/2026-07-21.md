# 2026-07-21 工作记录

## 运维指令集梳理
- 用户询问"目前需要指令集（命令行）操作的动作清单"。
- 核查结论：后端 `auth.js` 仅 login/me/logout，无用户增删改/改密码 API；前端无用户管理页。`users` 表结构 `id/username(UNIQUE)/password_hash/created_at`，密码 bcrypt 哈希。
- 产出 `docs/运维指令集.md`：汇总所有仅命令行动作（用户管理、PM2 服务、防火墙、备份迁移、外部导入），重点标注加用户/改密码/防火墙三条命令。
- 关键发现：`auth.js` 注释指向 `scripts/create-user` 脚本，但 `scripts/` 下并不存在 → 加用户目前需 node 直连 db。建议后续补 `scripts/create-user.js` 或 Web 用户管理页。
