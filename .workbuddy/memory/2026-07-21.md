# 2026-07-21 工作记录

## 运维指令集梳理
- 用户询问"目前需要指令集（命令行）操作的动作清单"。
- 核查结论：后端 `auth.js` 仅 login/me/logout，无用户增删改/改密码 API；前端无用户管理页。`users` 表结构 `id/username(UNIQUE)/password_hash/created_at`，密码 bcrypt 哈希。
- 产出 `docs/运维指令集.md`：汇总所有仅命令行动作（用户管理、PM2 服务、防火墙、备份迁移、外部导入），重点标注加用户/改密码/防火墙三条命令。
- 关键发现：`auth.js` 注释指向 `scripts/create-user` 脚本，但 `scripts/` 下并不存在 → 加用户目前需 node 直连 db。建议后续补 `scripts/create-user.js` 或 Web 用户管理页。

## M8 用户管理模块（已实装部署 + 推送 commit 52d7f86）
- 用户采纳建议：把「加用户 / 改密码」做成网页用户管理页。
- **后端** `server/src/routes/users.js`：GET 列表 / POST 新增 / POST me/password 改自己 / PUT :id/password 重置他人 / DELETE :id 删除（禁删自己）；bcrypt(cost10)；受 requireAuth 保护。
- **关键 bug**：初版误挂 `app.use("/api", usersRouter)` → router 内 `get("/")` 匹配 `/api/` 而非 `/api/users`，返回 404。修正为 `app.use("/api/users", usersRouter)`。（经验：Express 子路由挂载点要含完整前缀路径）
- **前端**：`pages/UserManagementPage.jsx`（列表/新增/重置/删除）+ `components/common/ChangePasswordDialog.jsx`（改自己密码，校验原密码）+ `api.users` 封装；App `/users` 路由 + Sidebar 菜单项（内联 SvgIcon，遵循 Vite8 禁 @mui/icons-material）+ Layout AppBar「修改密码」入口。
- 端到端验证全通过（登录/列表/新增/改密/重置/禁删自己400/删他人）。已 build 部署到 PM2 forge（3000），前端 /users 返回 200。
- **文档**：新增 `docs/modules/08-用户管理模块-PRD.md`（编号 M8，注意 M7 已是会议计划，故用户管理为 M8）+ 顶层 PRD 索引/修订记录 + CHANGELOG 2026-07-21 条目。

## M8 权限模型升级为 owner/admin/member 三级（已实装部署 + 推送 commit a54681c）
- 用户指出原"人人可管"设计不合理，采纳方案 B(role 字段)+②(bootstrap 指定主账号为 admin)+③(owner/admin/member 三级) 组合。
- **数据库** `server/src/db.js`：`users` 表 ALTER 加 `role` 列(`TEXT NOT NULL DEFAULT 'member'`)；迁移时把 `chenxu` 升为 owner（不可撼动的最高权限）。
- **后端**：`auth.js` login/me 返回 role；`routes/users.js` 新增 `requireAdmin`(owner/admin)/`requireOwner`(owner) 中间件，`/me/password` 所有人可用、其余套 requireAdmin，新增 `PUT /api/users/:id/role`（仅 owner，禁改 owner 角色、禁设为 owner）。
- **权限矩阵**：owner 不可被删/降级/重置密码；admin 可管 member 但不能碰其他 admin/owner；member 仅改自己密码、进不了用户管理页（侧边栏隐藏 + 入口守卫）。
- **前端**：`Sidebar.jsx` 仅 owner/admin 显示用户管理菜单；`UserManagementPage.jsx` 入口守卫 + 角色列(Chip) + 按角色禁用重置/删除 + owner 专属「修改角色」对话框；`api/client.js` 加 setRole。
- **验证**：端到端权限矩阵脚本 25/25 全通过（含越权被拒 403/400）；脚本临时接管并恢复 chenxu 密码、清理测试账号，零污染。
- **文档**：重写 08-PRD 为三级模型 + CHANGELOG 补条目 + 顶层 PRD M8 描述与修订记录更新。

### 经验沉淀（可复用）
- 最小权限原则落地：用 `role` 列 + 中间件(requireAdmin/requireOwner) 比"固定 admin 账号"更灵活（管理员可多个、不与账号名死绑）。
- 零污染端到端验证模式：脚本临时改管理员密码 → try{跑测试} → finally{恢复原密码 + 删测试数据}，避免碰真实账号。

## 缺陷排查：OA 浏览器插件导入物料「项目错配」（已修复 + 推送）
- **现象**：用户反馈"从浏览器插件 OA 导入【物料管理】不好使"。
- **根因**：M8 多用户隔离改造（767328c）给所有 `/api` 加 `requireAuth`，但 `auth-middleware.js` 白名单只放行了 `/materials/oa-import`、`/materials/oa-fetch`，**漏掉插件匹配项目用的 `GET /api/projects`** → 插件匿名调用被 401 → 拿不到项目列表 → 所有 OA 物料被塞进写死的 project 20（液冷超节点）。注：`oa-import` 接口本身正常（白名单内，返回 200 能落库），所以是"项目错配"而非"完全失败"。
- **修复**：
  1. 后端 `POST /api/materials/oa-import` 新增按 `internal_code`（内部立项号）/ `order_number` **服务端解析目标项目**，不再依赖插件调 `/api/projects`；解析优先级 internal_code → project_id → 报错；响应回显 `project_id`/`project_name`。
  2. 插件 `chrome-extension/inject.js` 移除 `/api/projects` 调用，改为上报已提取的「内部立项号」为 `internal_code`；成功提示显示项目名称。
  3. 用本机 Chrome 重新打包 `chrome-extension.crx`（chrome-extension.pem）。
- **验证**：curl 三种场景全对（按 701085 → project 6 HG9400整机柜；凭空立项号 → 报错；无立项号 → 回退 20）。
- **工具链经验**：可在 Git Bash 用 `"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --user-data-dir=临时目录 --pack-extension=扩展目录 --pack-extension-key=.pem` 重新打包 .crx（无 GUI，自动覆盖原 .crx）。
- **安全说明**：`oa-import` 仍属白名单公开接口（与 767328c 前一致），LAN 内知道立项号者可 POST；如需加固可后续加 OA 专用 token。

### Mantis Cookie 取用指南（给非 Chrome 同事）
- 结论：Mantis Cookie 取自**同事自己登录 Mantis 的浏览器**，与打开 Forge 用哪个浏览器无关 → Edge/Firefox/Safari 均可，无需 Chrome。
- 关键坑：Mantis 登录态 Cookie 通常是 `httpOnly`，JS 书签/`document.cookie` 读不到 → 必须走 DevTools → Network → 复制请求 Headers 里的 `Cookie` 整段值（已写 `docs/mantis-cookie-guide.md` 可转发）。
- 备选：右键请求「Copy as cURL」→ 取 `--cookie '...'` 段。
- Cookie 会随 Mantis 会话过期，过期后 Forge 报「鉴权失败」，需重取。
- **已内嵌进界面**：`MantisConnectionCard.jsx` 在 Cookie 框 helperText 加「？如何获取 Cookie」链接 → 弹出 Dialog（含各浏览器通用步骤 + httpOnly 关键坑 + cURL 备选）。前端已构建部署，无需另开文档。修复了 return 双根节点需 Fragment 包裹的 JSX 报错。

## M4 易用性：物料管理页内置「？OA 导入说明（Chrome 扩展）」弹窗
- 用户要求：物料管理页也加一个 chrome://extensions 的说明（类比 Mantis 的 Cookie 弹窗）。
- **实现** `client/src/pages/MaterialListPage.jsx`：工具栏新增「？ OA 导入说明」按钮 → 弹出 Dialog，图文说明 Chrome/Edge 扩展「Forge OA 物料导入」的安装（chrome://extensions → 开发者模式 → 加载已解压的扩展程序 / 拖入 .crx）、使用（OA 页点图标 → 提取 → 发送到 Forge → 按内部立项号归项目）、更新后需重载、仅支持 Chromium 内核（Firefox/Safari 不可用，已如实标注）。
- **注意**：OA 一键导入依赖 Chrome 扩展，仅 Chrome/Edge 可用；物料管理页本身无 OA 导入按钮（入口在 OA 网页的扩展图标）。页面原 `handleOaImportOpen/handleOaFetch` 等是未接 UI 的死代码，未触碰。
- 已构建部署，/materials 200，产物含 chrome://extensions 与「OA 物料导入」字样。CHANGELOG 已补。

## M1 项目计划：批量导入（本地 Excel / 腾讯文档）+ 一键清空；删除 PLM 功能
- **需求**：①【项目计划】一键清空当前计划；②删除 PLM 连接功能；③本地 Excel 导入（模糊识别表头 + 自动区分阶段/普通/节点任务）；④腾讯文档链接导入（同逻辑）。
- **选型（AskUserQuestion 确认）**：
  - 腾讯文档导入 = 「粘贴可下载链接」：用户在腾讯文档把表格设为可下载/任何人可阅读，复制链接粘到 Forge，后端 fetch → exceljs 解析首个工作表为矩阵 → 复用本地 mapScheduleMatrix（轻量、零 OAuth 依赖）。
  - 任务区分 = 「两者结合」：优先读「任务类型/类型」列值（阶段/里程碑/节点/普通）→ 回退按名称关键字（含「阶段」/「里程碑」「节点」）→ 默认普通；层级用 indentLevel/parent_id 栈还原父子，前置依赖按行号(1-based)或任务名映射。
- **后端新增**：`server/src/utils/scheduleMapping.js`（纯字符串处理，不耦合 Excel 库）：`buildFieldMap`/`detectTaskType`/`calcIndent`/`toDateStr`/`mapScheduleMatrix`；`FIELD_ALIASES` 覆盖 name/task_type/planned_start/planned_end/duration_days/predecessor/notes/indentLevel。
  - `routes/schedule.js` 新增三端点：`DELETE /projects/:id/schedule`（owner 校验后清空）、`POST /import`（收 {tasks} → transaction 批量插入）、`POST /import-from-url`（收 {url} → fetch+exceljs → mapScheduleMatrix → 批量插入）。
  - `insertScheduleTasks`：indentLevel 栈映射 parent_id（截断跨级）、`resolvePredecessors`（全数字→行号；否则名称匹配）、日期缺省回填。
- **后端删除 PLM**：`db.js` 两表 `plm_connection`/`plm_task_map` 由 CREATE 改为 `DROP TABLE IF EXISTS`；`index.js` 移除 `plmRouter` 挂载；删除 `routes/plm.js`、`adapters/plm.js`（仅 1 行无效探针配置 + 空映射，删无害）。
- **前端新增**：`utils/scheduleMapping.js`（与 server 同语义副本）、`utils/scheduleExcel.js`（`parseScheduleExcel` 用 XLSX + mapScheduleMatrix，无任务行抛明确错误）。
- **前端改动**：`SchedulePage.jsx` 工具栏加 [导入 Excel][腾讯文档导入][清空计划]（内联 SvgIcon，禁 @mui/icons-material）；隐藏 file input + 腾讯文档 Dialog + 清空确认 Dialog；删除 PLM 对话框。`api/client.js` schedule 块加 `clearAll/importTasks/importFromUrl`，删 `plm` 块。删 `components/plm/PlmConnectionDialog.jsx`。
- **验证**：临时用户端到端 14/14 通过（登录→导入5条含层级/阶段/节点/前置→列表确认 parent 映射→清空→越权403→腾讯文档链接导入含阶段识别→非法URL 400→清理零污染）。前端构建成功（dist 新 hash，forge online，/plans 200），产物含新文案、PLM 无残留。
- **文档**：M1 PRD 加 2.6.11 文件导入 / 2.6.12 一键清空；CHANGELOG 顶部 2026-07-21 条目；顶层 PRD 修订记录；删 `docs/plm-schedule-sync-assessment.md`。

### 经验沉淀（可复用）
- **腾讯文档轻量导入范式**：能粘贴可下载链接就别接 OAuth SDK —— 后端直接 fetch + exceljs 解析，复用本地映射函数，省去鉴权与回调工程。
- **前后端共享纯函数映射模块**：把「表头模糊识别 + 任务区分」抽成不依赖 Excel 库的工具（mapScheduleMatrix 吃二维数组），前端 XLSX/后端 exceljs 各取所长但共用同一套识别规则，避免逻辑分叉。
- **Excel 行号反查前置依赖**：用户常填「行号」而非任务名当前置，解析层优先按数字→行号映射，否则退名称匹配，体验更贴近手填习惯。

## M1 导入增强：开始/完成/工期三字段互推 + 模糊词条扩充
- **用户诉求**：导入 Excel 时阶段自动识别为阶段任务、任务为普通任务，且「开始时间/完成时间/工期」要自动对应、相关模糊词条都自动识别。
- **新增 `deriveDates(start,end,duration,taskType)`**（server + client 同名副本同步）：
  - 任意两者推导第三值：开始+完成→工期；开始+工期→完成；完成+工期→反推开始。
  - 阶段任务：交给子任务聚合；若显式给了开始+完成也先算工期。
  - 节点任务：单日里程碑（开始=完成，工期=1）。
  - 仅给工期：落库锚定到项目起始日（保证可渲染），后续可由前置级联调整。
- **模糊词条扩充**（`FIELD_ALIASES`/`TASK_TYPE_ALIASES`）：开始时间/开工日期/完成日期/截止时间/工期(天)/历时/紧前任务/任务类别/层级/备注说明 等大量中文常见写法。
- **修正 `insertScheduleTasks` 兜底 bug**：原逻辑「只有完成」时把开始直接设成完成、吞掉工期；改为依赖 `deriveDates` 输出并在服务端再做防御性补齐（开始+工期→完成、完成+工期→开始），使任何调用方传入欠指定任务也能正确推导。
- **验证**：① 单元 `mapScheduleMatrix` 14 项断言全过（各组合 + 关键字识别 + 模糊表头全匹配）；② 真实 API 端到端（临时用户登录→导入4条含「开始+工期/仅完成/仅工期/阶段」→GET 校验派生日期→清理零污染）。已 `npm run build` + `pm2 restart forge`（新产物 `index-Cz-BmtHY.js`，health 200）。
- **文档**：CHANGELOG 顶部条目补「日期三字段互推」「模糊词条扩充」；M1 PRD 2.6.11 表头别名表与任务区分规则更新，新增「开始/完成/工期三字段自动互推」段。

### 经验沉淀（可复用）
- **日期三角互推是排期导入的刚需**：用户手填 Excel 极少三字段齐全，应在映射层（mapScheduleMatrix）统一推导，前端预览与后端落库共用，避免预览/入库不一致；服务端 `insertScheduleTasks` 再补一道防御性兜底。
- **托管 Node 跑 ESM 脚本的坑**：`NODE_PATH` 对 ESM `import` 无效；裸模块要放在能解析到的 `node_modules` 下（如把脚本放 `server/`），或用 `createRequire(import.meta.url)` 绝对路径加载；`node:fetch` 在此构建不可导入，直接用全局 `fetch`。

## M1 导入缺陷修复：真实模板（项目计划模板.xlsx）表头/阶段列适配
- **用户实测反馈**：用桌面 `项目计划模板.xlsx` 导入后「时间都不对」。复现定位根因：模板表头 `阶段/任务/计划开始时间/计划完成时间/工期（天）` 全部未命中精确别名 → 三个日期列 null → 时间全空。
- **修复（`scheduleMapping.js` server + client 副本同步）**：
  - `norm()` 增加**全角→半角归一**（`（）`→`()`、全角短横、全角空格 U+3000）；新增**「包含关键字」兜底**（含 开始/完成/工期/阶段/前置/备注/层级/类型/任务 即匹配），彻底兼容 `计划开始时间/计划完成时间/工期（天）` 等。
  - `FIELD_ALIASES` 扩充 `计划开始时间/计划完成时间/计划开工/计划结束时间` 等；新增 `phase` 字段认 `阶段` 列。
  - **阶段列→阶段任务分组**：`阶段` 列有值 → 合成「阶段任务」父节点，其下任务自动挂为子节点（indent 1）；空白行归属上一非空阶段。后端 `recalcPhaseAggregation` 已存在，按 `parent_id` 聚合出阶段起止时间（无需额外改动）。
- **修复腾讯文档路径日期丢失**：`import-from-url` 原 `cell.value` 处理遇 Date 单元格返回空串；新增 `cellValueToStr` 助手（Date→本地 YYYY-MM-DD，富文本/公式取文本）。
- **验证**：单元映射 + 真实接口端到端（登录→用真实模板跑 import→GET 阶段聚合）通过——33 条任务、4 个阶段（计划/详细设计/研发测试/试制）正确识别、日期全部正确、阶段聚合正确、临时数据零残留。已 build + restart forge（health 200）。
- **文档**：CHANGELOG 顶部新增「2026-07-21（补）M1 导入缺陷修复」；M1 PRD 2.6.11 表头表加「阶段分组」行 + 新增「阶段列分组」「表头容错增强」两段。

### 经验沉淀（可复用）
- **导入表头匹配要做「全角归一 + 包含兜底」**：中文 Excel 模板千奇百怪（全角括号、带「计划」前缀），纯精确别名总会漏；加全角→半角归一 + 关键字包含兜底，命中率从「全空」变「全中」。
- **「阶段列」是常见模板语义（分组），不是每行类型**：很多模板用独立 `阶段` 列做分组标签（阶段名下任务不重复写阶段名），应识别为「合成阶段任务父节点 + 子任务挂接」，而非把阶段列当 task_type 值。后端已有聚合逻辑时，前端只需产出正确 parent 关系即可。
