# 2026-07-11 工作日志

## PLM 项目排程同步 — 技术可行性评估（架构评审，未编码）
- 需求：HPM【项目计划】排程（阶段/里程碑/甘特图任务）推送到 plm.sugon.com（达索 3DEXPERIENCE/ENOVIA v6 混合平台）对应项目排程，实现"改 HPM → PLM 跟着变"。
- 结论：⚠️ 条件可行。最优路线 A = 3DSpace REST 直连（★★★）；保底路线 D = 导出 Excel/JSON → 人工导入（永远可用）。
- 已落盘评估文档：`docs/plm-schedule-sync-assessment.md`
- 关键前提确认进展：
  - **P-1 网络可达性 ✅ 已确认**（用户 10:29 回复"能访问"）
  - P-2 账号（用户问"怎么查看" → 已回复两种查法：①服务账号/API凭证问PLM管理员；②SSO Cookie 复用，浏览器 DevTools 复制 Cookie 头，同 mantis.js）
  - P-3 PLM 排程 schema（类型/字段，待 P0 只读探针探明，非阻塞）
- 复用现有 adapter 模式（参照 mantis.js）：新增 `adapters/plm.js` + `routes/plm.js` + `plm_connection`/`plm_task_map` 表
- 实施阶段：P0 连接配置+只读探针 → P1 单向推送闭环 → P2 增量同步+前端"推送"按钮
- 注：本次仅架构评估与方案设计，未进入编码阶段。

## 待办（用户确认前提后再定）
- 等用户回复 P-1/P-2/P-3 后，决定是否进入 P0 探针实现。

## P0 已实现并通过验证（2026-07-11）
- 用户 10:37 提供 CAS 登录 Cookie：`JSESSIONID=...cas01`（选路线① SSO Cookie 复用，免审批）。
- 主理人实测探测：HPM 服务端可出网连 PLM（TLS 需跳过内部 CA，curl 报 exit60→加 -k 通）；平台确认为**经典 ENOVIA v6**（`/3dspace/api/` 返回 404、emxNavigator/emxUICore 等 jsp 特征）；Cookie 有效（带 Cookie 进应用层，未踢回 CAS 登录页，裸 jsp 缺参报 500 错误页）。
- 实现（工程师 IS_PASS: YES；QA PASS 7/7，路由 NoOne）：
  - `server/src/db.js`：新增 `plm_connection`（连接配置）+ `plm_task_map`（预留）两表
  - `server/src/adapters/plm.js`：`PlmAdapter`（getConfig/saveConfig/_httpRequest 默认 rejectUnauthorized=false/probe 结构化返回）
  - `server/src/routes/plm.js`：`GET/PUT /api/plm/connection` + `POST /api/plm/probe`（错误映射 网络/TLS→502，参数→400）
  - `server/src/index.js`：`app.use("/api", plmRouter)` 注册
  - `client/src/api/client.js`：新增 `plm` 接口对象
  - `client/src/components/plm/PlmConnectionDialog.jsx`：连接配置表单 + 探针结果结构化展示
  - `client/src/pages/SchedulePage.jsx`：项目计划页头部加「PLM 连接/探针」按钮打开 Dialog
- 实测：探针带 Cookie 连 PLM `emxTree.jsp` 返回 500 错误页（PLM 侧缺参数，证明 HPM→PLM 链路通）；连接配置已写入 DB（id=1）。
- 提交：35e52d7 已推送 origin/main

## PLM 排程真实接口逆向（2026-07-11 用户 F12 抓包 + 主理人实测）
- **关键发现：排程数据接口 = `POST /3dspace/common/emxFreezePaneGetData.jsp`**（非 REST，是 v6 jsp servlet）。用户 F12 抓到真实请求，并发来「Copy as curl」。
- **必带鉴权/头**（缺则失败）：
  - Cookie：`JSESSIONID=...cas01`（CAS SSO 会话，会过期需重贴）+ `testcookie=1` + `afs=<uuid>`
  - **CSRF Token**：请求头 `ENO_CSRF_TOKEN: R70H-SY1E-...-IMPP`，且配套 `csrfTokenName: ENO_CSRF_TOKEN`（缺则 PLM 拒写/异常）
  - `Origin` / `Referer` 必须同源 `https://plm.sugon.com`（Referer 是完整 `emxIndentedTable.jsp?...` 长 URL，含 tableMenu=PMCWBSTableMenu、portal=PMCSchedule、mode=PMCWBS、suiteKey=ProgramCentral、parentOID、objectId 等表定义参数）
  - `Content-Type: text/plain`（用户 curl 显示 undefined，实测 text/plain 可）
- **请求参数**（query string）：`fpTimeStamp=<ms>`、`objectId=<项目OID 点分格式 如 19520.49557.60552.29879>`、`firstTime=true`、`toolbarData=expandLevel=1|emxExpandFilter=1`、`IsStructureCompare=`、`sortColumnName=PhaseEstimatedStartDate`、`sortDirection=ascending`。**POST body 为空（Content-Length:0）**。
- **实测结果**：
  - 仅 GET / 缺 CSRF → HTML 500 错误页（`内部服务器错误`）。
  - 带齐 Cookie+CSRF+POST 原样重放 → 返回 **XML**（`<mxRoot><action>error</action><message>java.lang.NullPointerException</message>`），栈在 `com.matrixone.apps.framework.ui.UITableIndented.getXMLByOID(...:2702)`。说明**认证/接口/路径全对**，但 `getXMLByOID` 因缺某前置参数/会话状态 NPE。
  - 直接 `GET emxIndentedTable.jsp` 也 **HTTP 500 空响应** → 证实 PLM 排程页**强会话状态依赖**（须先在浏览器完成导航建立 session 表格配置，才能取数；无法用裸 curl 独立 bootstrap）。
- **推论 / 待解**：
  - 读路径：因 session 状态依赖，HPM 独立 HTTP 取数需复刻浏览器完整导航序列（或抓到 getData 的真实 Response 结构）。
  - **写路径（P1 核心目标）尚未探明**：需用户在 PLM 里真实"新增/编辑一条 WBS 任务"，用 F12 抓该动作的「Copy as curl」→ 这才是 P1 要复刻的写接口与字段映射。
- **下一步（已向用户要）**：
  - ① 用户在浏览器 Network「Response」标签复制 `emxFreezePaneGetData.jsp` 的**真实返回 XML**（含任务行/字段）→ 直接给出字段映射，无需我复刻请求。
  - ② 用户在 PLM 新增/编辑一条 WBS 任务 → 「Copy as curl」发我 → 拿到写接口与参数。
  - 注：用户截图 objectId 出现过两种格式（连字符 `1920-4655-...` 与 点分 `19520.49557....`），疑似不同对象（任务 vs 项目），需以真实抓包为准。

## 用户决策：PLM 同步功能「先预留，后续继续开发」（2026-07-11 11:38）
- 用户原话："这个功能先预留吧，后续继续开发，你可以记入 memory 待办中"。
- **状态变更**：PLM 排程同步整体由"进行中"转为"预留/暂停"，计入 memory 待办。
- 已完成：P0（连接配置表 + 只读探针 + 前端连接对话框）已交付并提交 35e52d7，链路/鉴权/真实接口已验证打通。
- 待后续：P1（单向推送闭环，向 PLM 写 WBS 任务）+ P2（增量同步 + 前端「推送」按钮）。
- **P1 启动前置卡点（仍待用户提供）**：
  - ① 在浏览器 PLM 真实「新增/编辑一条 WBS 任务」→ F12 复制该保存请求的「Copy as curl」发我（写接口 + 字段映射）；
  - ② 或在 Network「Response」复制 `emxFreezePaneGetData.jsp` 的真实返回 XML（读结构/字段映射）。
  - 根因：ENOVIA v6 强 session 依赖，裸 curl 无法独立 bootstrap 取数/写数。
- 注意：连接配置已落库（plm_connection id=1），SSO Cookie 会过期，续做时需重新向用户索取。Cookie 严禁硬编码进源码（已用连接配置表 + 对话框输入模式）。

## 甘特图增量开发交付（2026-07-11）
- 需求：在【项目计划】排期表下方，根据已有排期生成甘特图并体现任务依赖关系（FS）。
- 流程（增量 SOP）：现状调研(Explore) → 架构师高见远增量设计(`docs/gantt-chart-design.md` + `docs/gantt-class-diagram.mermaid` + `docs/gantt-sequence-diagram.mermaid`) → 工程师寇豆码实现(T1-T6) → QA 严过关验收(Round1+Round2)。
- 实现：新增 `client/src/components/schedule/GanttChart.jsx`(主容器) / `GanttTimeline.jsx`(月+周双行表头刻度) / `GanttRow.jsx`(任务名列+条形) / `GanttLinks.jsx`(SVG FS 依赖箭头)；修改 `client/src/pages/SchedulePage.jsx`(排期表后插入 `<GanttChart tasks={tasks} />` + 横向滚动)。纯前端、**零新增依赖自绘**(dayjs+MUI+SVG)，复用 `schedule_tasks` 现有字段(`predecessor_ids` 已支持 FS 依赖)，不改后端/数据模型。
- 特性：月+周双行 sticky 表头与网格、条形按 bg_color/completion_status 着色、阶段任务加粗描边、hover Tooltip、今天红色参考线、FS 依赖箭头(SVG marker，情形A/B 折线防穿越)、空态 EmptyState、横向滚动；**本期只读不拖拽**。
- QA：Round1 构建+算法 46/46 全过，发现 1 缩进 Bug(`GanttRow.jsx:54` depth 缩进被 MUI 按 8px 间距单位误乘) → 路由工程师修复(commit `59dfe48`，改显式 `paddingLeft:{8+depth*16}px`)；Round2 回归全过，路由 NoOne，无遗留。
- 提交：`c68e1fd`(实现) + `59dfe48`(修复缩进) 已 push origin/main；`CHANGELOG.md` 已追加甘特图条目。
- QA 建议：项目无组件测试框架(仅 vite)，后续可补 vitest + @testing-library/react 固化 7-9 类边界便于回归。

## 项目计划三项增量开发交付（2026-07-11）
- 需求：① 阶段任务可折叠（排期表 + 甘特图联动）② 甘特图时间轴单位可切换（日/周/月/季度）③ 模版排期表各 M 级阶段加背景色并存入模版。
- 流程（增量 SOP）：现状调研(Explore) → 架构师高见远设计(`docs/schedule-enhance-design.md` + `docs/class-diagram.mermaid` + `docs/sequence-diagram.mermaid`) → 工程师实现(T1 折叠联动 / T2 单位切换 / T3 模板存色 / T4 边界自测) → QA 严过关验收(Round1 + Round2)。
- 关键实现点：
  - 折叠：collapsedPhases 上提 SchedulePage；`computeVisibleTasks` 提取到 `client/src/utils/schedule-date.js`；`visibleTasks` 同喂 ScheduleTable + GanttChart，两处联动（阶段自身恒显、仅隐子孙）。
  - 单位：`PX_PER_DAY = {day:24, week:12, month:4, quarter:1.5}` 缩放模型（px/天随单位递减，真正缩放非仅换标签）；GanttChart 加 `unit` prop；GanttTimeline 改通用 major/minor 双行表头；`dayjs.extend(quarterOfYear)`。
  - 模板存色：模板 JSON 任务加 `bg_color`；新增 `POST /api/templates/schedule`（保存/同名 upsert，文件名 CJK + 路径穿越防护，循环依赖 DFS 三色 400）+ `GET /api/templates/schedule/:file`（编辑预填）；`generate` INSERT 透传 `bg_color`；新增 `TemplateEditorDialog.jsx`（复用 `ContextMenu.PRESET_COLORS` 调色板）；`custom-hardware.json` 的 M1–M5 及嵌套阶段补高区分度底色。
- QA Round1：构建无回归 + 前端算法 45/45 断言全过；发现 2 处后端缺陷 → 路由工程师。Round2(commit `46e6fb9`) 修复并回归：GET 中文名模板读回 200 + POST 路径穿越字符 `../` 显式 400，后端 8/8 通过，路由 NoOne，无回归。
- 提交：`fadc8db`(三项实现) + `46e6fb9`(修复两缺陷) 已 push origin/main；`CHANGELOG.md` 已追加条目。
- 注意：后端改完需 `npx pm2 restart hpm-server` 才会生效（QA 验收踩过"陈旧进程"坑，新端点未加载）。
- 后续：① 补 vitest + @testing-library/react 组件测试（折叠联动/单位坐标/模板对话框）；② 模板 DELETE 端点未真正物理删除（仅返回成功），如需"模板管理-删除"产品能力要补真实实现；③ 甘特图拖拽改期未做。

## 崩溃残留恢复（2026-07-11 收尾）
- 工程师在修 Round1 缺陷时 Agent 运行时崩溃（非代码 bug），残留未提交改动被 `git stash`（标注 recoverable）。收尾时已 `git stash apply` 安全恢复：本 memory 日志、MEMORY.md、模块 PRD(`docs/modules/04-物料管理模块-PRD.md`) 及上一轮甘特图未提交的设计文档(`docs/gantt-*.md`)。stash 不含 schedule.js，故未覆盖已修好的后端。恢复后已 drop stash。
