# 2026-07-11 工作日志

## PLM 项目排程同步 — 技术可行性评估（架构评审，未编码）
- 需求：HPM【项目计划】排程（阶段/里程碑/甘特图任务）推送到 plm.sugon.com（达索 3DEXPERIENCE/ENOVIA v6 混合平台）对应项目排程，实现"改 HPM → PLM 跟着变"。
- 结论：⚠️ 条件可行。最优路线 A = 3DSpace REST 直连（★★★）；保底路线 D = 导出 Excel/JSON → 人工导入（永远可用）。
- 已落盘评估文档：`docs/plm-schedule-sync-assessment.md`
- 关键前提确认进展：
  - **P-1 网络可达性 ✅ 已确认**（用户 10:29 回复"能访问"）
  - P-2 账号（用户问"怎么查看" → 已回复两种查法：①服务账号/API凭证问PLM管理员；②SSO Cookie 复用，浏览器 DevTools 复制 Cookie 头，同 mantis.js）
  - P-3 PLM 排程 schema（类型/字段，待 P0 只读探针探明，非阻塞）
- 复用现有 adapter 模式（参照 mantis.js）：新增 `adapters/plm.js` + `routes/plm.js` + `plm_connection`/`plm_task_map` 表
- 实施阶段：P0 连接配置+只读探针 → P1 单向推送闭环 → P2 增量同步+前端"推送"按钮
- 注：本次仅架构评估与方案设计，未进入编码阶段。

## 待办（用户确认前提后再定）
- 等用户回复 P-1/P-2/P-3 后，决定是否进入 P0 探针实现。

## P0 已实现并通过验证（2026-07-11）
- 用户 10:37 提供 CAS 登录 Cookie：`JSESSIONID=...cas01`（选路线① SSO Cookie 复用，免审批）。
- 主理人实测探测：HPM 服务端可出网连 PLM（TLS 需跳过内部 CA，curl 报 exit60→加 -k 通）；平台确认为**经典 ENOVIA v6**（`/3dspace/api/` 返回 404、emxNavigator/emxUICore 等 jsp 特征）；Cookie 有效（带 Cookie 进应用层，未踢回 CAS 登录页，裸 jsp 缺参报 500 错误页）。
- 实现（工程师 IS_PASS: YES；QA PASS 7/7，路由 NoOne）：
  - `server/src/db.js`：新增 `plm_connection`（连接配置）+ `plm_task_map`（预留）两表
  - `server/src/adapters/plm.js`：`PlmAdapter`（getConfig/saveConfig/_httpRequest 默认 rejectUnauthorized=false/probe 结构化返回）
  - `server/src/routes/plm.js`：`GET/PUT /api/plm/connection` + `POST /api/plm/probe`（错误映射 网络/TLS→502，参数→400）
  - `server/src/index.js`：`app.use("/api", plmRouter)` 注册
  - `client/src/api/client.js`：新增 `plm` 接口对象
  - `client/src/components/plm/PlmConnectionDialog.jsx`：连接配置表单 + 探针结果结构化展示
  - `client/src/pages/SchedulePage.jsx`：项目计划页头部加「PLM 连接/探针」按钮打开 Dialog
- 实测：探针带 Cookie 连 PLM `emxTree.jsp` 返回 500 错误页（PLM 侧缺参数，证明 HPM→PLM 链路通）；连接配置已写入 DB（id=1）。
- 提交：待推送（与 CHANGELOG、评估文档一并 commit）
